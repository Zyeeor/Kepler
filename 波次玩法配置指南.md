# 波次玩法配置指南

本文说明如何配置波次玩法（当前主玩法），覆盖**配置结构、字段说明、配置流程、常见坑**，与最新实现（2026-08-18）一致。

**核心配置链路**：`WaveManager`（波次节奏 + 整体模式）→ `waves`（每波 WaveConfig：编队表 + 数量/时长）→ `WaveDefEntry`（编队条目：def + 本波权重）→ `MonsterWaveDef`（怪物编队资产：刷哪些怪、组内数量）→ 怪物 prefab。

需求来源：正式设计 `.vibe/doc/Canonical/01_DESIGN_CANONICAL.md`（§15 Run/Wave/Final）；相关：《地图生成与流送配置指南.md》（地图/刷怪衔接）。

---

## 1. 配置结构总览

```text
WaveManager（场景根对象，单例）
  ├─ waveMode          ← 整体波次模式（全局统一，不逐波配）
  ├─ groupScatterRadius / spawnInterval / choiceBuffer / autoShowChoiceUI / recycleRemainingOnTimeUp
  ├─ waves: List<WaveConfig>   ← WaveManager 直接持有并执行
  │     ├─ weightedTable: List<WaveDefEntry>
  │     │     ├─ def: MonsterWaveDef（资产，可跨波复用）
  │     │     └─ weight（本波内占比，每波独立）
  │     ├─ totalCount（数量波） / duration + maxSpawnCount（时间波）
  │     └─ doublePick（本波清场后选卡：单/双）
  └─ gracePeriod / autoStart / autoStartTimeout
```

**关键原则**：
1. **模式整体统一**：`waveMode` 只有一个（CountKill 或 Timed），设定上"全杀怪计数"或"全计时"，不逐波混用。
2. **权重在波条目上**：同一 `MonsterWaveDef` 可被多波引用，每波独立配 `weight`（占比），互不影响。
3. **def 只描述"刷什么怪"**：怪物组成（prefab + 组内数量）+ 静态怪权重；**数量由波层控制**（totalCount / duration / maxSpawnCount）。

---

## 2. WaveManager（波次主控，场景根对象）

| 字段 | 默认 | 含义 |
|---|---|---|
| `waveMode` | CountKill | **整体波次模式**：CountKill=全部波为数量波；Timed=全部波为时间波 |
| `groupScatterRadius` | 3 | **怪物群系**：同一编队（WaveDef 一组怪）刷出时围绕同一中心点的散射半径（米）。组内怪聚集出现（≈"一群怪"），中心点在玩家 B 带内合法位置 |
| `spawnInterval` | 0.5 | 波次补刷间隔（秒）：每隔多久按权重表抽一批（一个编队）刷出 |
| `choiceBuffer` | 2 | 波完成 → 选卡前的缓冲（秒），用于看清战果；选卡期间 timeScale=0 冻结此等待 |
| `autoShowChoiceUI` | true | 波完成时自动打开选卡弹窗（CoreChoiceUI 负责防重入） |
| `recycleRemainingOnTimeUp` | true | 时间波结算时回收本波剩余在场怪（不写快照，永久退场） |
| `gracePeriod` | 2 | 首波开始前准备时间（秒） |
| `autoStart` | true | 场景加载后自动启动波次（等待地图流送就绪后开刷） |
| `autoStartTimeout` | 30 | 自动启动前等待地图流送就绪的最长时间（秒），超时仍强开（刷怪点自动重试） |

**常用事件**（脚本订阅用）：`OnWaveStarted(waveIndex, config)`、`OnWaveCompleted(waveIndex)`、`OnAllWavesComplete`、`OnWaveEnemyKilled(actor)`；`CurrentWaveIndex` / `EnemiesAlive` / `IsWaveActive` / `TimeWaveRemaining` 供 UI 读取。

---

## 3. 每波配置（WaveConfig）

> 检查器由自绘 Drawer 条件显示：Timed 模式显示 `duration`+`maxSpawnCount`，CountKill 模式显示 `totalCount`，`doublePick` 恒显示。

| 字段 | 模式 | 默认 | 含义 |
|---|---|---|---|
| `weightedTable` | 全部 | 空 | **编队表**：`List<WaveDefEntry>`，本波按条目 `weight` 抽取刷怪（见 §4） |
| `totalCount` | CountKill | 20 | 本波刷怪**总数**：刷满后不再补，玩家清完场上本波怪触发选卡 |
| `duration` | Timed | 60 | 本波时长（秒）：时间到即结算（触发选卡），剩余在场怪按 `recycleRemainingOnTimeUp` 回收 |
| `maxSpawnCount` | Timed | 0 | 本波**累计刷怪总数上限**（含已击杀/回收）：0 = 不限制（仅受 MonsterSpawner 全场配额 `maxCombatMonsters` 约束）；刷满后不再补刷，计时继续 |
| `doublePick` | 全部 | false | 本波清场后选卡：true=双选（连续选 2 张），false=单选 |

**选卡时点**：数量波 = 清场；时间波 = 到时（或刷满 maxSpawnCount 后等时间走完）。波完成后 `choiceBuffer` 缓冲 → 弹 `CoreChoiceUI`（timeScale=0）→ 选完 → 下一波自动开始。

---

## 4. 编队条目（WaveDefEntry）

```text
weightedTable: [ { def: MonsterWaveDef, weight: 1.0 }, ... ]
```

| 字段 | 默认 | 含义 |
|---|---|---|
| `def` | 空 | 怪物编队资产（`MonsterWaveDef`，见 §5）。同一 def 可被多波引用 |
| `weight` | 1 | **本波内该编队的抽取权重（占比）**：值越大越常出。仅本波生效，不影响其他波引用同一编队 |

> **权重语义（重要）**：`weight` 决定"每批抽到哪个编队"的**概率**，不是最终怪物数量的精确比例。抽中一个编队会刷出它的整组（组内 count 只），组越大单次贡献越多。要精确控制构成 → 把目标组合放进**一个 def** 里。

---

## 5. 怪物编队（MonsterWaveDef，资产）

创建：`Assets → Create → Kepler/Map → Monster Wave`（放 `Assets/Settings/MapStreaming/Waves/` 统一管理）。

| 字段 | 默认 | 含义 |
|---|---|---|
| `id` | 空 | 配置唯一 id（仅地图静态怪模式去重用；波次模式不参与） |
| `spawnWeight` | 1 | **仅地图静态怪模式**（`ChunkDef.waveTable`）使用；波次玩法不用此字段 |
| `monsters` | 空 | 怪物组成：`prefab`（必须挂 MonsterActor）+ `count`（组内数量）。**抽中本编队时整组刷出** |
| `statMult` | 1/1/1 | 属性倍率（难度调节预留），当前不应用 |

**怪物 prefab**：`Assets/Prefabs/Monster/`（pride_new / sloth_new / gluttony_new / Envy(full) / Wrath(full) / Greed（full）等），必须挂 `MonsterActor`。

**单怪 vs 多怪**：
- 单怪 def（如 `PrideWaveDef`：pride ×1）≈ 直接配"一种怪"，权重即该怪占比；
- 多怪 def（如 pride ×2 + sloth ×1）= 固定搭配编队，抽中整组一起出（群系聚集）。

---

## 6. 配置流程（从零到跑通）

### 步骤 1：准备怪物编队资产
1. `Assets → Create → Kepler/Map → Monster Wave`；
2. `monsters` 添加条目：拖怪物 prefab + 设组内数量；
3. 命名规范：`怪物名WaveDef`（如 `PrideWaveDef.asset`）。

### 步骤 2：场景挂 WaveManager 并配整体
1. 场景根建空物体 `WaveManager`，挂 `WaveManager` 组件；
2. 选整体模式 `waveMode`（CountKill / Timed）；
3. 调节奏：`groupScatterRadius`（群系半径）、`spawnInterval`（补刷间隔）、`gracePeriod`（首波准备）。

### 步骤 3：配置每波
1. `waves` 添加条目（波数 = 你要的波数）；
2. `weightedTable` 添加条目：拖 `def` + 设本波 `weight`；
3. 按模式填 `totalCount`（数量波）或 `duration` + `maxSpawnCount`（时间波）；
4. 需要双选的波勾 `doublePick`（如末波 W8）。

### 步骤 4：运行验证
1. Play → 首波自动开始（有地图时等流送就绪）；
2. 数量波：清完场上本波怪 → 缓冲 → 选卡 → 下一波；
3. 时间波：倒计时（`TimeWaveRemaining`）到点 → 选卡；
4. 全波完成 → `UIManager.ShowResult(true)` 弹 VICTORY 结算面板。

---

## 7. 常见坑

| 现象 | 原因 | 处理 |
|---|---|---|
| 波一直不开始 | `autoStart=false` 或地图流送未就绪 | 检查 `autoStart` / 地图流送状态 |
| 抽不到怪 / 波直接跳过 | `weightedTable` 空或条目 `def` 为空 | 检查每波编队表引用 |
| 权重改了没用 | 改在 def 的 `spawnWeight` 上（那是静态怪权重） | 波次权重在 `WaveDefEntry.weight` 上改 |
| 数量波刷不满 totalCount | 全场配额满（`maxCombatMonsters`=30）或刷怪点不足 | 调高配额 / 检查 `TryGetWaveSpawnPosition` 日志 |
| 怪散落全图 | 群系半径 `groupScatterRadius` 过大 | 调小（如 3） |
| 时间波不补刷了 | 达到 `maxSpawnCount` 上限 | 调大上限或设 0（不限） |
| 选卡没弹 | `autoShowChoiceUI=false` | 打开 `autoShowChoiceUI` |
| W8 想双选没生效 | `doublePick` 未勾在目标波 | 在目标波开启 `doublePick` |

---

## 8. 当前实际配置（EnemyAiTest）

| 项 | 配置 |
|---|---|
| 整体模式 | `waveMode=CountKill`（数量波） |
| 群系 | `groupScatterRadius=3` |
| W1 | PrideWaveDef ×3（totalCount=3） |
| W2 | SlothWaveDef ×4 |
| W3 | Pride + Gluttony ×6 |
| W4 | Sloth + Gluttony ×8 |
| W5 | Envy ×10 |
| W6 | Wrath ×12 |
| W7 | Greed ×14 |
| W8 | Wrath + Greed + Envy ×16，**doublePick=true** |
| 缓冲/间隔 | choiceBuffer=2、spawnInterval=0.5、gracePeriod=2 |
| 选卡 | autoShowChoiceUI=true（无 RoomFlow 兜底） |

*文档由 WorkBuddy 整理于 2026-08-18，基于当前仓库实现与正式设计对照生成。*
