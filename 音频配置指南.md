# 音频配置指南（Audio Configuration Guide）

> 日期：2026-08-18（v2：系统已激活，实测可行）
> 状态：✅ 音频系统已激活、主要链路已接线并通过实测（合成测试资产占位，正式资产接入后覆盖即可）
> 关联：设计真源 `.vibe/doc/Canonical/03_PRESENTATION_CANONICAL.md` §28 Audio；实现 `Assets/Scripts/Systems/Audio/AudioManager.cs` + `SceneBgm.cs`

---

## 1. 音频系统总览（三路分离）

游戏音频分为三路，互不干扰，音量各自独立调节：

| 路 | 用途 | 对应设置面板音量 |
|---|---|---|
| BGM | 背景音乐（单源循环，切换时自动淡入淡出 1s） | 音乐 |
| SFX | 战斗/世界音效（并发池，最多约 6 个同时响） | 音效 |
| UI | 界面音效（按钮 / 弹窗 / 选卡） | UI |

特性：

- 跨场景常驻：由常驻 GameManager 统一创建，切场景时音乐不中断、全局唯一；
- 音量三键独立持久化（设置面板调整后即时生效）；
- 未配置音效（槽位为空）时静默跳过——**不会报错，这是设计行为**；
- 编辑器切后台会暂停音频，回前台自动恢复（正常行为，不是 bug）。

## 2. 当前状态（已激活）

- ✅ **系统已激活**：AudioManager 由常驻 GameManager 自动创建（无需任何场景挂载），场景加载自动重连事件订阅（防漏挂）、自动自愈重建；
- ✅ **BGM 场景切换已接线**：`SceneBgm` 组件（见 §6），主菜单 ↔ 对局场景已配测试曲，切换自动 CrossFade；
- ✅ **UI 音效已接线**：任意 UI 点击 → 通用点击音；选卡弹窗 → 弹卡/选卡/重抽专属音；
- ✅ **已接线的音效槽位**（AudioClip 拖入即用 / 替换即换）：

| 玩法时刻 | 配置位置 | 当前资产 |
|---|---|---|
| 波次开始（仅第 1 波） | AudioManager `waveStartSfx` | `Assets/Audio/SFX/sfx_wave_start.wav`（测试） |
| 每波清场 | AudioManager `waveClearSfx` | `Assets/Audio/SFX/sfx_wave_clear.wav`（测试） |
| 全部波次完成 | AudioManager `waveAllCompleteSfx` | `Assets/Audio/SFX/sfx_wave_all.wav`（测试） |
| 附身成功 | AudioManager `possessionStartSfx` | `Assets/Audio/SFX/sfx_possess.wav`（测试） |
| 通用 UI 点击 | AudioManager `uiClickSfx` | `Resources/Audio/UI/ui_click.wav` 兜底（测试） |
| 弹卡 / 选卡 / 重抽 | CoreChoiceUI `cardOpenSfx` / `cardSelectSfx` / `cardRerollSfx` | `Assets/Audio/UI/ui_card_open|select|reroll.wav`（测试） |
| 场景 BGM | 场景内 SceneBgm `bgmClip` | 主菜单 `bgm_menu.wav`、对局 `bgm_test.wav`（测试） |

> 测试资产均为程序合成占位（音色简陋），**正式资产就位后直接替换同名文件或改字段引用即可，无需改代码**。

## 3. 策划自助配置

### 3.1 资产放置与命名规范（建议约定）

| 类型 | 目录 | 命名前缀 | 示例 |
|---|---|---|---|
| 音乐 | `Assets/Audio/BGM/` | `bgm_` | `bgm_combat_main` |
| 战斗/世界音效 | `Assets/Audio/SFX/` | `sfx_` | `sfx_wave_clear` |
| 界面音效 | `Assets/Audio/UI/` | `ui_` | `ui_card_select` |

导入设置：BGM 类勾选 **Loop（循环）**；其余保持默认即可。

### 3.2 三种配置方式

- **方式 A（已有槽位）**：把 AudioClip 拖到对应字段 / 替换测试资产，即插即用（见 §2 表格）；
- **方式 B（场景 BGM）**：挂 `SceneBgm` 组件 + 拖一个 clip（见 §6），全程无需代码；
- **方式 C（新事件 / 新需求）**：交给 AI 接入，见 §5。

## 4. P0 音效清单（设计真源 §28）

设计要求的 P0 事件共 17 类，接入状态：

| 事件 | 玩法时刻 | 状态 |
|---|---|---|
| Wave Start / Clear | 波次开始 / 清场 | ✅ 已接（Wave Start 当前仅首波=对局开始语义，与策划对齐确认中，见 §7 待办） |
| 全部波次完成 | 8 波打完 | ✅ 已接 |
| Possession Start | 附身成功 | ✅ 已接 |
| UI 通用点击 | 任意 UI 点击 | ✅ 已接（本轮新增） |
| Card Open / Select / Reroll | 弹卡 / 选卡 / 重抽 | ✅ 已接（本轮新增） |
| 场景 BGM | 主菜单 / 对局 | ✅ 已接（SceneBgm） |
| Possession Complete / 换身 | 附身完成 / 主动换身 | 🔲 系统侧可接（AI 加） |
| Corpse Window | 波清场后 2-3s 窗口 | 🔲 系统侧可接 |
| Soul Enter / Death / Fail | 灵魂态 / 灵魂死亡 | 🔲 系统侧可接 |
| Bullet Time Start / End | 子弹时间 | 🔲 系统侧可接 |
| Final Begin / Phase Change / Clear | Final 压力战 | 🔲 系统侧可接（Final 尚未实现） |
| Body 受击 / 临界耐久 | 被打 | 🔲 战斗侧（另一负责人） |
| Enemy Fatal / Corpse Available | 击杀 / 出尸体 | 🔲 战斗侧 |
| Target Lock | 锁定目标 | 🔲 战斗侧 |
| Ability Use / Hit | 技能使用 / 命中 | 🔲 战斗侧（接入方案见 §9） |
| Movement | 移动 | 🔲 战斗侧 |
| Elite Spawn | Elite 出现 | 🔲 战斗侧 |
| Hazard | 场景危害 | 🔲 战斗侧 |

> 策划提需求时如涉及"战斗侧"行，AI 会明确告知需另一负责人配合，避免误解。

## 5. 与 AI 沟通的规范流程（核心）

### 5.1 一句话需求模板

> 「XX（事件/场景）加 BGM/音效：文件在哪个路径，风格/情绪，是否循环，切换时机」

### 5.2 提供信息清单（一次给全，AI 免追问）

1. **触发时机**：哪个事件 / 哪个场景 / 哪个按钮；
2. **音频资产**：已放进 `Assets` 的文件路径（AI 不凭空生成正式音频，资产需策划/音频提供）；
3. **风格 / 情绪**（可选）：如"紧张战斗感""空灵""胜利号角"；
4. **是否循环**（BGM 默认循环，音效默认不循环）；
5. **特殊要求**（可选）：音量大小、淡入淡出时长、是否 3D 空间定位（随距离衰减）。

### 5.3 策划可预期的 AI 处理路径

| 需求类型 | AI 会做什么 |
|---|---|
| 已有槽位 | 直接配置资产 / 替换测试占位，不改代码，你立即可用 |
| 新事件音效 | 找到事件触发点 → **事件源处直调一行** `AudioManager.Instance.PlaySfx/PlayUiSfx(...)`（直调优先，事件源最知时机与上下文）；仅事件源需保持音频无感知时才由 AudioManager 订阅 |
| 场景 BGM | 挂/配置 `SceneBgm` 组件即可（见 §6），无需代码 |
| 战斗侧事件 | 明确告知属于战斗侧，需另一负责人对接 |

> 接入规范（2026-08-18 审核确立）：**默认直调**（CoreChoiceUI 已验证此模式）；
> 批量接入前先落 SfxId+SfxBank 统一通道（见 §8），届时新增音效 = enum 加一行 + 资产拖 clip + 触发点一行，AudioManager 零改动。

### 5.4 可直接复制的示例

- 已有槽位：`波次开始的音效换成正式的，文件在 Assets/Audio/SFX/sfx_wave_start_final.wav，紧张感`
- 新事件：`选卡界面播放卡片音效，Assets/Audio/UI/ui_card_open.wav，清脆；选卡确认再播 ui_card_select.wav`（已接，替换即可）
- 场景 BGM：`主菜单换正式曲，Assets/Audio/BGM/bgm_menu_final.wav 循环；对局换 bgm_combat.wav，淡入淡出 1 秒`

## 6. 场景 BGM 扩展流程（游戏场景与主菜单不同）

### 6.1 需求场景

主菜单一首曲子，进入对局自动换成战斗曲，结算再换——**曲目跟随场景/流程自动切换**。

### 6.2 方案 A：SceneBgm 组件（✅ 已落地，策划自助最高）

- 每个场景挂一个 `SceneBgm` 组件（任意对象上），填 `bgmClip` 字段即可；
- 进场景自动播放该曲（留空 = 保留上一场景音乐不切换）；
- 切换自动 CrossFade（默认 1s），同曲不重启；
- 收益：以后策划想给任何场景换 BGM = **挂组件拖一个 clip**，无需再找 AI。

### 6.3 方案 B：阶段驱动（正式做法，未实现，按玩法流程切）

- 对齐设计 §28 的 BGM 六套：普通战斗 / 波清场·选卡 / Elite / 灵魂态 / Final / 结算；
- AI 把 BGM 槽位接到整局流程阶段链上（波次中 / 选卡时 / 死亡 / 结算），阶段切换自动 CrossFade；
- 收益：音乐情绪随玩法节奏走，比"按场景切"更贴合正式版；
- 给 AI 的需求话术：`按方案 B 做阶段 BGM：普通波播战斗曲、选卡播缓和曲、结算播胜利曲，文件在 Assets/Audio/BGM/`

### 6.4 验证方式

- 听感验证：切场景时 1s 淡入淡出、音乐不中断；
- 设置面板：三路音量独立调节即时生效；
- 日志验证：切换时控制台有 BGM 切换日志。

## 7. 常见问题

| 现象 | 原因与处理 |
|---|---|
| 配置了音效但没声音 | ① 槽位为空=静默（正常）；② **编辑器切到后台会暂停音频，回前台恢复**（最常见假象）；③ 音量被调成 0；④ 场景缺启用的 AudioListener（每个场景的 Main Camera 需有，且只能有一个启用——GameManager 会自动修正） |
| 音效同时响太多、太吵 | SFX 并发池上限约 6，找 AI 调大 `sfxPoolSize` |
| BGM 切场景不切 | 目标场景没挂 `SceneBgm`（无组件 = 保留上一场景音乐，属设计行为） |
| BGM 切到后台停了 | 正常：编辑器后台暂停音频，回前台自动恢复 |
| 音量设置不生效 | 三键独立持久化；改设置面板后即时生效 |
| 替换测试音 | 直接替换 `Assets/Audio/` 下同名文件，或改字段引用新资产，无需改代码 |

---

## 8. 批量接入路线（SfxBank 统一通道）

剩余约 10 类 P0 事件（系统侧 6 + 战斗侧 7）批量接入前，先落地统一通道避免返工：

1. **SfxBank（ScriptableObject）**：`SfxId enum + 资产映射表`（id→clip→volumeScale→minInterval），AudioManager 收敛为纯播放器 `Play(SfxId, pos)`（查表+节流+池调度，不再认识任何事件源）；
2. **节流已内建**（当前 AudioManager 已实现 per-clip 60ms 最小间隔 + 池轮询调度）；
3. **迁移顺序**：机制层 → 现有 7 事件搬进 SfxId → 系统侧 6 类（Soul/子弹时间/Corpse Window，挂点现成）→ 战斗侧 10 类随战斗负责人节奏直调；
4. **判定"不返工"**：新增一类音效时 AudioManager 零改动。

## 9. 攻击/技能音效接入说明（战斗侧）

> 预期形态：玩家与怪物的不同攻击/技能将触发**各自不同的音效**（如普攻、技能释放、命中、蓄力）。
> 当前**未实现**（战斗侧属另一负责人），本节省略方案，供接入时直接执行。

### 9.1 当前音频管理已具备的扩展条件（无需改动）

| 能力 | 说明 |
|---|---|
| `AudioManager.PlaySfx(clip, worldPos, volumeScale)` | **支持 3D 空间定位**（传世界坐标=随距离衰减，技能/受击天然适配）；不传坐标=2D 全向 |
| per-clip 节流（60ms） | 多段攻击/高频命中自动合并，**不会爆音**（无需调用方关心） |
| SFX 池轮询调度 | 并发上限 6，全忙时均匀覆盖，不卡死 |
| 直调规范（§5.3） | 战斗侧可直接一行调用，无订阅包袱 |

### 9.2 推荐接入方式：能力基类字段（最小侵入，战斗负责人实施）

在 `EnemyAbility` / `PlayerAbility` 两个基类各加：

```csharp
[Header("Audio")]
[Tooltip("技能释放音效；null 静默。")]
public AudioClip abilitySfx;          // 释放/起手
[Tooltip("命中音效；null 静默。")]
public AudioClip hitSfx;              // 命中/受击
```

- **释放音**：在 `SpawnVfx()`（基类已有，释放特效时机）旁边加一行：
  `if (abilitySfx != null) AudioManager.Instance?.PlaySfx(abilitySfx, transform.position);`
- **命中音**：在各能力的命中点（如 `SpawnVfxTracked(hitImpactPrefab, ...)` 处）加：
  `if (hitSfx != null) AudioManager.Instance?.PlaySfx(hitSfx, hitPos);`
- 每个具体能力类（如 `EnemyAbility_SwordQi`）只需在检查器拖自己的 clip，**互不干扰、各配各的**；
- 若某类怪物共用一套音效，可在怪物 prefab 根上配置后由能力基类读取（按需演进）。

### 9.3 可选进阶：SfxBank 统一通道（§8）

若战斗音效数量膨胀（20+ 能力 × 多段），启动 §8 的 SfxId enum + SfxBank SO 映射表：
能力基类字段从 `AudioClip` 换成 `SfxId`，查表播放；届时 AudioManager 收敛为纯播放器，新增音效不再改代码。

### 9.4 给战斗负责人的一句话交接

> 战斗音效挂点 = 能力基类 `SpawnVfx()`（释放）+ 命中点（受击）；直调 `AudioManager.Instance?.PlaySfx(clip, worldPos)`，节流与池已内建，不用处理并发。

## 10. 待办与已知语义（策划/负责人确认）

- **Y7 Wave Start 语义**：当前仅第 1 波发声（= 对局开始音）。确认"每波都要"则去掉首波守卫；保留则改名 `runStartSfx`。
- **正式资产替换**：全部测试合成音（`Assets/Audio/`）待替换，替换同名文件或改字段引用即可。

---

*本文档为团队协作指南；实现细节与代码变更记录见工程侧 `.docs/`。*
