# 音频配置指南（BGM / 音效 / UI / 旁白语音 全攻略）

> 日期：2026-08-24（v3：合并叙事旁白配置，一份文档覆盖全部音频）
> 状态：✅ 音频系统 + 叙事旁白系统均已落地，全部可通过 Unity 资产配置，无需改代码
> 关联：设计真源 `.vibe/doc/Canonical/03_PRESENTATION_CANONICAL.md` §28 Audio；`.vibe/doc/Canonical/Content/Narrative_Voice_Delivery_Baseline_v1.0.md`

---

## 1. 先认识四种声音

游戏里的声音分四类，音量各自独立（设置面板四个滑块：音乐 / 音效 / UI / 语音），互不干扰：

| 类别 | 是什么 | 例子 | 设置面板滑块 |
|---|---|---|---|
| BGM | 背景音乐，同一时间只有一首，切换自动淡入淡出（默认 1 秒） | 战斗曲、结算曲 | 音乐 |
| 音效 SFX | 战斗 / 世界的短音效，多个可同时响 | 波次开始、附身成功 | 音效 |
| UI 音效 | 界面点击 / 弹窗声，走独立通道，不会被其他音效挤掉 | 按钮点击、选卡 | UI |
| 旁白 Voice | 叙事语音，自动配字幕，播放时自动压低 BGM | 开场欢迎、精英登场台词 | 语音 |

三条全局约定（所有音频通用）：

- **没配就静默**：任何槽位留空，游戏正常进行、不报错——这是设计行为，方便你逐步填充，不是 bug；
- **换了资产就换声音**：正式音频到位后，替换 clip 引用即可，绝大多数情况不需要改代码；
- **配置入口唯一**：BGM 阶段曲 / 音效 / 旁白录音都从 **AudioManager** 出发（见 §2），事件自动接线由挂在其上的控制脚本完成，不用碰代码。

> 当前仓库里配的是程序合成的测试音（音色简陋），正式资产就位后按本文档换掉即可。

---

## 2. 全部配置入口一张表

**先记住唯一入口：AudioManager。** 绝大多数音频配置都从它出发：

> 打开 `AudioManager`（对局场景里的 AudioManager 对象，或 `Assets/Resources/Audio/AudioManager.prefab`）→ 点开对应字段里的资产 → 改内容。
> 它的身上同时挂着音频控制脚本（AudioEventBinder / NarrativeScheduler 及四个通道控制器），这些由系统自动运转，不需要手动配置。

| 要配什么 | 从哪里出发 | 最终改的地方 |
|---|---|---|
| 音效（SFX / UI）的音频 | AudioManager → `SfxBank` 字段点开 | `Assets/Resources/Audio/SfxBank.asset` |
| 阶段 BGM（对局内细分曲） | AudioManager → `StageBgmMap` 字段点开 | `Assets/Resources/Audio/StageBgmMap.asset` |
| 旁白录音文件登记 | AudioManager → `VoiceClipSet` 字段点开 | `Assets/Resources/Audio/VoiceClipSet.asset` |
| 怪物技能施放音 | AudioManager → `MonsterSkillAudioConfig` 字段点开 | `Assets/Resources/Audio/MonsterSkillAudioConfig.asset` |
| 场景兜底 BGM | **SceneBgm** 组件 | 挂在场景内任意物体上 |
| 旁白说什么、何时说 | **NarrativeCue** 资产 | `Assets/Resources/Narrative/Cues/` 文件夹下新建 |
| 启用哪些旁白 | **NarrativeCueSet** 资产 | `Assets/Resources/Narrative/NarrativeCueSet.asset` |
| 旁白字幕文字 | **TextCatalog** 资产 | `Assets/Resources/Text/TextCatalog.asset` |
| 旁白进度规则（A0→A4） | **NarrativeAccessProfile** 资产 | `Assets/Resources/Narrative/NarrativeAccessProfile.asset` |
| 首通剧情 | **FirstClearConfig** 资产 | `Assets/Resources/Narrative/FirstClearConfig.asset` |
| 神龛音效开关 | **PossessionBodyProvider** 组件 | 神龛雕像 prefab 上 |

音频文件存放目录建议：

| 类型 | 目录 | 命名前缀 | 示例 |
|---|---|---|---|
| 音乐 | `Assets/Audio/BGM/` | `bgm_` | `bgm_combat_main` |
| 音效 | `Assets/Audio/SFX/` | `sfx_` | `sfx_wave_clear` |
| UI | `Assets/Audio/UI/` | `ui_` | `ui_card_select` |
| 旁白 | `Assets/Audio/Voice/` | `voice_` | `voice_welcome_01` |

导入设置：BGM 类勾选 **Loop（循环）**；其余保持默认即可。

---

## 3. 怎么配 BGM（背景音乐）

BGM 分两层：**场景曲（兜底）** 和 **阶段曲（对局内细分）**。仲裁规则很简单：

> 结算/失败（终态）> 灵魂态/精英（覆盖槽）> 阶段槽（含逐波曲）> 场景曲。
> 对局中进入某个阶段，自动切到该阶段曲子；该阶段没配（Inherit）就保持当前音乐不动（不静音、不重播）。

### 3.1 场景曲：SceneBgm 组件

- 在场景任意物体上添加 `SceneBgm` 组件，把音乐拖到 `bgmClip` 字段；
- 进入场景自动播放；字段**留空 = 保持上一场景的音乐**（主菜单进对局不配阶段曲时，就一直播主菜单曲）；
- 典型用法：主菜单配一首、对局场景配一首兜底。

### 3.2 阶段曲：StageBgmMap 资产（含逐波配置）

在 AudioManager 上点开 `StageBgmMap` 字段（即 `Assets/Resources/Audio/StageBgmMap.asset`）。

**① 逐波 BGM（waveTiers 列表）——战斗曲的主要配置方式：**

| 字段 | 含义 |
|---|---|
| waveNumber | 玩家侧第几波（从 1 开始） |
| slot.action | 行为：Inherit 保持 / Play 播放 / Stop 停止（逐波一般用 Play） |
| slot.clip | 该波的曲子 |
| slot.fadeOverride | 该波切换的淡入淡出秒数（0 = 用全局 1 秒） |

规则很简单：**该波配了条目 → 进波时按 action 处理（Play 切曲 / Inherit 保持 / Stop 停曲）；没配 → 保持当前曲不切**。
例：只配 Wave 1 和 Wave 5 → 第 1 波切 A 曲，2~4 波继续 A，第 5 波切 B 曲，6~8 波继续 B。

> 列表留空 = 回退旧行为（所有波次共用 combat 槽一首曲）。

**② 其余槽位（每个都有 action 三态）：**

| 槽位 | 什么时候响 | 常见配法 |
|---|---|---|
| combat | waveTiers 为空时的统一波次曲 | Play + 曲子 |
| choice | 波清场 · 选卡 | Inherit（保持战斗曲） |
| final | Final 压力战 | Play + 曲子 |
| result | 结算（胜利） | **Stop（结算时淡出停止）** |
| fail | 结算（失败） | **Stop（结算时淡出停止）** |
| soul | 灵魂态（离身期间） | Play + 曲子 |
| elite | 精英波 | Play + 曲子 |

- **action 三态**：`Inherit` 保持当前曲 / `Play` 播放 clip（clip 空会警告并退回 Inherit）/ `Stop` 淡出停止（结算用）；
- 每个槽位可单独设 `fadeOverride`（淡入淡出秒数）；填 0 或不填 = 用全局 1 秒；
- 优先级：**结算/失败（终态）> soul/elite 覆盖槽 > 阶段槽（含逐波曲）> 场景曲**。终态期间灵魂态/精英曲不会盖过结算行为。

---

## 4. 怎么配音效（SFX / UI）

### 4.1 一个资产管所有音效：SfxBank（按四个分区显示）

在 AudioManager 上点开 `SfxBank` 字段（即 `Assets/Resources/Audio/SfxBank.asset`）。编辑器里按**四个分区**显示，一眼看清每类音效：

| 分区 | 是什么 | 挂点状态 |
|---|---|---|
| 游戏事件音效 | 波次开始/结束、附身、子弹时间、结算、神龛等 | 已接线（AudioEventBinder），只需拖 clip |
| UI 界面音效 | 按钮点击、选卡 | 已接线，只需拖 clip |
| 战斗音效 | 受击、击杀、移动、危害等 | 战斗负责人直调，拖 clip 即生效 |

（怪物技能音效不在此资产，独立文件见 §4.2。）

每个条目字段含义：

| 字段 | 含义 | 怎么填 |
|---|---|---|
| id | 音效位的名字（程序定的） | 不用动 |
| clip | 音频文件 | 拖入 / 替换 |
| volumeScale | 相对音量倍率（0~2） | 想小声填 0.5，大声填 1.5 |
| minInterval | 最短重播间隔（秒） | 0 = 不限制；怕刷屏填 1 |
| pitch | 音高（0.5~1.5） | 变调用 |
| channel | World = 世界音效 / Ui = 界面音效 | 按条目预设即可 |
| prefer3D | 是否 3D 定位（离声源越远越小声） | 世界音效默认勾 |

**换音效 = 找到对应条目、换 clip**，就这么简单。

> 全部音效位（含战斗侧预留位）的条目都已**预建好**：有测试音的已配上，暂无素材的留空（静默）。
> 后续正式音频到位 = **找到条目 → 拖入 clip**，不需要新增条目，不需要改代码。

### 4.2 怪物技能音效（独立文件，按怪配）

怪物技能音效**不在 SfxBank 里**，单独一个文件：`Assets/Resources/Audio/MonsterSkillAudioConfig.asset`（也可从 AudioManager 的 `MonsterSkillAudioConfig` 字段点开）。SfxBank 已移除技能音效类别（三分区），技能音效配置统一走本资产。

打开后按七种怪分区显示，每个怪固定三行：

| 行 | 含义 |
|---|---|
| 位移 | 该怪位移技能（如冲刺、飞行）的施放音 |
| 普攻 | 普通攻击的施放音 |
| 技能 | 专属技能（如激光、锁链风暴）的施放音 |

**配置三步**：打开资产 → 找到怪的折叠区 → 在对应行拖入音源（可调音量/音高）。留空 = 静默。

- 触发时机：怪物发动该技能时自动播放（3D 定位），AI 怪和玩家附身怪都会播；
- 怪的身份已预设：七个怪 prefab 的 `sinType` 字段已填好，策划不需要动；
- 特殊情况：某个**单个能力**想用不一样的音，可在该能力预制体上填 `castAudioName`（下拉选择），它会覆盖上面这行的默认音。

### 4.3 已接线的音效时刻（都可在 SfxBank 里换）

| 游戏时刻 | SfxBank 条目名 | 备注 |
|---|---|---|
| 波次开始 | WaveStart | 默认首波响，可改成每波响（AudioManager 上的 AudioEventBinder 组件 `waveStartEveryWave` 开关） |
| 波次清场 | WaveClear | |
| 全部波次完成 | AllWavesComplete | |
| 附身成功 | PossessionStart | |
| 主动离身 | PossessionEnd | |
| 附身身体死亡 | PossessBodyDied | |
| 清场后出尸体窗口 | CorpseWindow | |
| 进入灵魂态 | SoulEnter | |
| 灵魂死亡 | SoulDeath | |
| 子弹时间开始 / 结束 | BulletTimeStart / BulletTimeEnd | |
| Final 开始 / 通关 | FinalBegin / FinalClear | |
| 精英出现 | EliteSpawn | |
| 神龛接近 / 提供躯体 | ShrineProximity / ShrineProvide | 见 §4.4 |
| 按钮点击 | UiClick | UI 通道 |
| 弹卡 / 选卡 / 重抽 | CardOpen / CardSelect / CardReroll | UI 通道 |

### 4.4 神龛音效

神龛（雕像 prefab）上的 `PossessionBodyProvider` 组件：

- `audioEnabled`：神龛音效总开关（默认开）；
- `audioProximityRadius`：接近提示圈半径（默认 3 米；填 0 与触发器同半径）。

灵魂首次靠近提示圈 → 播 ShrineProximity 提示音；成功提供躯体 → 播 ShrineProvide 音。音频同样在 SfxBank 换。

---

## 5. 怎么配旁白（叙事 Voice）

> **调度器（NarrativeScheduler）本身不用配**：它挂在 AudioManager 对象上，唯一字段 config 留空即可——参数资产（`NarrativeSchedulerConfig.asset`）已在 Resources 里配好默认值并自动加载。策划的日常工作只围绕下面的"三样东西 + 三个辅助资产"。

旁白 = 玩家推进过程中自动响起的**语音 + 字幕**。由三样东西配合：

1. **录音文件** → 登记进 `VoiceClipSet`（给每条录音起一个 audioId 名字）；
2. **说什么、何时说** → 新建 `NarrativeCue` 资产；
3. **字幕文字** → 写进 `TextCatalog`（给每句字幕起一个 textKey 名字）。

### 5.1 第一步：把录音登记进 VoiceClipSet

在 AudioManager 上点开 `VoiceClipSet` 字段（即 `Assets/Resources/Audio/VoiceClipSet.asset`）。资产里已经**预建了 12 条模板条目**（audioId 已起好、音源留空），照模板填即可：

| 字段 | 含义 | 怎么填 |
|---|---|---|
| audioId | 录音的唯一名字，Cue 里引用它 | 模板已起好，新条目按 `<场景>_<序号>` 命名 |
| mythic | 神谕风录音（空灵、威严） | 拖入神谕版录音 |
| system | 系统电子风录音（合成、冷静） | 拖入系统版录音 |

预建的模板条目（audioId → 对应的游戏时刻）：

| audioId | 游戏时刻 |
|---|---|
| welcome_run_01 | 对局开始欢迎 |
| carrier_intro_01 | 开场载体附身 |
| first_card_01 | 首次确认选卡 |
| first_elite_01 | 首只精英登场 |
| elite_fatal_01 | 精英阵亡 |
| soul_enter_01 | 回到灵魂态 |
| wave_clear_01 | 波次清场 |
| final_begin_01 | Final 开始 |
| run_won_01 / run_failed_01 | 胜利 / 失败 |
| first_clear_01 | 首通专属台词 |
| ambient_01 | 低压氛围语 |

**配置动作**：找到对应条目 → 往 mythic（和/或 system）拖入录音文件。两种风格都录了就都填，只录一种填一种；两个都空 = 静默（正常）。播放用哪种由"显示模式"决定（见 5.4）。

### 5.2 第二步：新建 Cue 资产（何时说、说什么）

在 `Assets/Resources/Narrative/Cues/` 文件夹右键 → Create → Kepler → Narrative → Cue，填以下内容：

**① cueId**：给这条旁白起个唯一名字（如 `welcome_01`）。不要重复。

**② triggers（什么时候说）**：一行一个触发条件组，选一个事件（如 `RunStarted` 对局开始），可设"第几次发生才说"（nth），还可附加条件（如"剧情进度 ≥ A2"）。
- 多行 triggers = 满足任意一行就触发；
- 一行内的多个条件按 join（And = 都满足 / Or = 任一满足）组合。

可选事件见 §5.6 事件清单。

**③ requiredAccess（剧情进度要求）**：低于该档位不触发。剧情进度 A0→A4 随玩家推进自动上升（规则见 §5.7）。

**④ advanceAccessOnComplete / accessResult**：播完这条后，把剧情进度推进到某档（可选，附加推进方式）。

**⑤ lines（内容行，说什么）**：
- 一行 = 一句；多句旁白就写多行，按顺序播放；
- 每行三个字段：`speaker`（用哪种配音风格）、`textKey`（字幕文字 key，对应 TextCatalog）、`audioId`（录音 key，对应 VoiceClipSet）；
- `gapAfter`：这句说完后停几秒再说下一句（默认 0.3）。

**⑥ subtitleMode（字幕模式）**：
- `None`：不出字幕；
- `Optional`：玩家在设置里开了字幕才显示；
- `Forced`：强制显示（重要台词用）。

**⑦ 行为字段**（一般保持默认，需要再调）：

| 字段 | 含义 | 默认 |
|---|---|---|
| delaySeconds | 触发后等几秒再说 | 0 |
| priority | 优先级，高的打断低的 | Normal |
| repeatScope | Repeatable 可反复说 / OncePerRun 每局一次 / OncePerProfile 整个存档一次 | Repeatable |
| minimumIntervalSeconds | 同一条旁白最少间隔几秒才能再说 | 0 |
| busyPolicy | 正有旁白在播时：Queue 排队等 / DropIfBusy 放弃 / Interrupt 插队打断 | Queue |
| bgmDuck | 播放期间自动压低 BGM（说完自动恢复） | 开 |
| deferUnderPressure | 高压战斗时刻自动延后再说 | 开 |
| interruptPolicy | 被更高优先级打断后：AbandonSequence 放弃整组 / ResumeAfterInterruption 等会儿接着没说完的继续 | 放弃整组 |

### 5.3 第三步：把字幕文字写进 TextCatalog

打开 `Assets/Resources/Text/TextCatalog.asset`，新增条目：

| 字段 | 含义 |
|---|---|
| key | 名字（对应 Cue 里的 textKey） |
| text | 中性版本文字（必填，兜底） |
| mythicText | 神谕风文案 |
| systemText | 系统电子风文案 |

三套文本按需填写；要与配音风格呼应就分别填。

### 5.4 两种配音风格何时用谁

- 全局默认：剧情进度 A0-A3 → 神谕风（Mythic），A4 → 系统风（System），可在 `NarrativeDisplayProfile` 资产调整；
- 个别 Cue 播完后可切换某个界面的文本风格（Cue 的 ⑮ 字段），一般不用动。

### 5.5 第四步：把 Cue 加进清单

打开 `Assets/Resources/Narrative/NarrativeCueSet.asset`，把新建的 Cue 资产拖进 `cues` 列表。**不在此列表的 Cue 不会播放**。

### 5.6 触发事件清单

| 事件 | 什么时候发生 |
|---|---|
| RunStarted | 对局开始 |
| InitialCarrierAssigned | 开场载体附身完成 |
| RunPhaseChanged | 阶段切换（可指定阶段名） |
| WaveStarted / WaveCompleted | 第 N 波开始 / 结束 |
| CardOffered / CardConfirmed | 抽卡呈现 / 玩家确认选卡 |
| PossessionStarted | 附身成功 |
| VoluntaryRelease | 主动离身 |
| SoulEntered | 回到灵魂态 |
| EliteSpawned / EliteFatal / ElitePossessed | 精英出现 / 精英死亡 / 附身精英 |
| RunWon / RunFailed | 胜利 / 失败 |
| Custom | 自定义事件（需程序配合埋点） |

可用条件：`RunPhaseIs`（当前阶段是 X）、`AccessAtLeast`（进度 ≥ X）、`WaveIndexAtLeast`（波数 ≥ N）、`ProfileFirstCleared` / `ProfileNotFirstCleared`（该存档是否已首通）、`PressureFree`（当前非高压时刻）。

### 5.7 剧情进度 A0→A4 怎么推进

默认规则（可在 `NarrativeAccessProfile` 资产改）：

```text
开场载体附身（第 1 次）      → A1
第一次确认选卡               → A2
第一次精英出现（且已 ≥ A2）  → A3
进入 Final 阶段              → A4
```

也可以让某条 Cue 播完后推进（Cue 的 ④ 字段）。

### 5.8 首通剧情

玩家首次通关时自动播一段八步剧情（引导、倾向评分、声明等），配置在 `FirstClearConfig` 资产（文案 key、选项、节奏）和 `TendencyScoreConfig` 资产（评分权重）。次通只走普通结算 + 一条可配的首通专属台词（用 `ProfileFirstCleared` 条件触发）。

### 5.9 现成配方（照抄改 ID 就行）

| 想要的效果 | 配置 |
|---|---|
| 开场欢迎语 | 触发 RunStarted 第 1 次 + repeatScope = OncePerRun + 单行神谕风 + Forced 字幕 |
| 首只精英登场双线台词 | 触发 EliteSpawned 第 1 次 + 条件 AccessAtLeast = 2 + 两行（神谕→系统）+ High 优先级 |
| 高压时氛围语（不急） | priority = Low + deferUnderPressure 开 |
| 首通专属台词 | 条件 ProfileFirstCleared + repeatScope = Repeatable + 设最小间隔 |

### 5.10 调度器参数与进度规则（一般不用动，需要再调）

**① 调度参数**（`Assets/Resources/Narrative/NarrativeSchedulerConfig.asset`）：

| 参数 | 含义 | 默认 |
|---|---|---|
| maxPendingCues | 排队等待的旁白最多几条（超出直接放弃） | 3 |
| pendingExpireSeconds | 排队超过几秒就过期丢弃 | 20 秒 |
| lowBodyHealthThreshold | 身体耐久低于多少算"高压时刻" | 30% |
| eliteNoDisturbSeconds | 精英出现后几秒内不打扰 | 4 秒 |
| pressureInFinal / DuringTransfer / DuringBlockingUi | 哪些时刻算高压（延后低优先级旁白） | 全开 |
| charsPerSecond | 无录音行的字幕按每秒几字计时 | 12 字/秒 |
| subtitleClamp | 无录音行字幕停留的最短/最长秒数 | 1.5 ~ 6 秒 |

**② 剧情进度规则**（`NarrativeAccessProfile.asset`）：控制 A0→A4 什么时候推进，已配好 4 条默认规则（开场附身→A1、首次选卡→A2、首只精英→A3、进 Final→A4）。想改推进节奏改这里，与 Cue 无关。

**③ 文本风格映射**（`NarrativeDisplayProfile.asset`）：决定神谕风/系统风何时用谁（默认 A0-A3 神谕、A4 系统），一般不动。

**④ 首通剧情**（`FirstClearConfig.asset` + `TendencyScoreConfig.asset`）：首次通关的八步剧情文案与倾向评分权重。

> 调参验证用 **F12** 叙事调试面板：强制进度档位、手动触发 Cue、看"为什么不触发"的决策原因。

---

## 6. 常见需求速查（我想做 X）

| 我想… | 怎么做 |
|---|---|
| 换波次开始音 | SfxBank → WaveStart 条目 → 换 clip |
| 主菜单换音乐 | 主菜单场景的 SceneBgm 组件 → 换 bgmClip |
| 换战斗曲（按波次） | StageBgmMap → `waveTiers` 列表 → 对应波次的 clip 换掉 |
| 所有波次共用一首战斗曲 | StageBgmMap → 清空 `waveTiers` → combat 槽拖 clip |
| 灵魂态想有专属曲 | StageBgmMap → soul 槽 → Play + 拖 clip |
| 结算失败想用另一首曲子 | StageBgmMap → fail 槽 → Play + 拖 clip（Inherit 则沿用 result 行为） |
| 精英波想换曲 | StageBgmMap → elite 槽 → Play + 拖 clip |
| 换按钮点击声 | SfxBank → UiClick 条目 → 换 clip |
| 让某个音效小声点 | SfxBank → 该条目 → volumeScale 调小 |
| 某个音效响得太频繁 | SfxBank → 该条目 → minInterval 设 1 秒 |
| 加一条开场旁白 | §5 四步：VoiceClipSet 登记录音 → 新建 Cue → TextCatalog 写字幕 → 加进 CueSet |
| 让某句旁白不出字幕 | Cue → subtitleMode = None |
| 让旁白不压低 BGM | Cue → bgmDuck 关 |
| 某条旁白整个存档只说一次 | Cue → repeatScope = OncePerProfile |
| 一句旁白被别的话顶掉后想接着说完 | Cue → interruptPolicy = ResumeAfterInterruption |

---

## 7. 调试与验证

- **F11**：音频调试面板（非正式流程下）——查看 SfxBank 缺失清单、四路音量、试播音效；
- **F12**：叙事调试面板（非正式流程下）——强制设置剧情进度 A0-A4、手动触发某条 Cue、模拟事件计数、查看"某条旁白为什么不触发"的决策原因；
- 快速听感验证：进一局 → 改资产 → 重新触发事件听效果。

---

## 8. 常见问题

| 现象 | 原因与处理 |
|---|---|
| 配了音效但没声音 | ① 槽位留空 = 静默（正常）；② 编辑器切到后台会暂停音频，回前台恢复；③ 音量被调到 0；④ 场景 Main Camera 缺 AudioListener（系统会自动修正） |
| 条目已拖 clip 但依然没声音 | ① 确认拖的是 SfxBank 里正确的条目（id 别选错）；② channel 是否与预期一致（UI 音效应是 Ui）；③ 用 F11 面板"强制触发"试播同一 id 验证 |
| 音效同时响太多、太吵 | 找 AI 调 SFX 并发上限 |
| BGM 切场景不切 | 目标场景没挂 SceneBgm（无组件 = 保持上一首，设计行为） |
| 旁白不触发 | ① Cue 没加进 NarrativeCueSet；② requiredAccess 高于当前进度；③ repeatScope 已用掉；④ 触发条件不满足——F12 面板看决策原因 |
| 旁白有字幕但没声音 | VoiceClipSet 里对应 audioId 没登记 clip，或当前配音风格对应的通道为空 |
| 字幕显示方框 | 字幕文字没写进 TextCatalog，或所用字体缺字（中文请用动态字体） |
| 两条旁白撞车 | 用 priority 定先后、busyPolicy 定撞车行为 |

---

## 9. 什么时候需要找 AI / 程序

以下情况需要程序配合（其余全部资产自助）：

- **要一个新的事件触发点**（§4.3 / §5.6 清单里没有的时刻）：如"第 X 次使用某技能时说话"；
- **战斗侧音效**（受击、击杀、移动、锁定、场景危害）：挂点归战斗负责人，音频侧接口已就绪（战斗负责人直接对接即可；怪物技能音效已由 MonsterSkillAudioConfig 接管，见 §4.2）；
- **自定义旁白事件**：需要在玩法里埋 `Custom` 事件点；
- **换正式音频**：直接换 clip 即可，无需程序。

---

*本文档为团队协作指南；实现细节与代码变更记录见工程侧 `.docs/`。*
