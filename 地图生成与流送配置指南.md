# 地图生成与流送配置指南

本文说明如何基于 Tile-based Map Streaming 框架配置无缝大地图与怪物生成，覆盖**架构、关键概念、具体配置步骤**，与最新实现（2026-08-14）一致。

**核心配置链路**：`ChunkDef`（地块模板）→ 地形 prefab（挂 `TileVisual` 玩法组件）→ `MonsterWaveDef`（怪物编队）→ 怪物 prefab；场景挂 `MapStreamingSystem` + `MonsterSpawner` 即跑通全链路。

需求来源：`.docs/Project/04_ENGINEERING_QA/` 下设计文档（MapStreaming_Design、MapStreaming_Tile_Arrangement_Proposal、MapStreaming_WorldPlan_Proposal）。
参考实现：`Assets/Settings/MapStreaming/`（已配好一份可跑示例）。
快速测试：见 §6 调试验证（Gizmos 色块 + F2 调试 HUD + Console 日志）。

---

## 0. 框架速览

```text
MapStreamingSystem（每 tickInterval 重算 A/B/C/D 范围集合 → Diff → 任务队列）
    ├─ 进 C → Prepare      ：生成 TileData 网格 + 出入口校验（纯逻辑，ChunkTileGenerator）
    ├─ 进 B → Instantiate  ：→ Dormant（ChunkVisualizer 铺地形方块 + MonsterSpawner 刷怪/快照恢复）
    ├─ 进 A → Activate     ：→ Active（怪物 AI 激活，追击/战斗）
    ├─ 离开 A → Pause      ：→ Dormant（AI 休眠）
    ├─ 离开 B → UnloadScene：→ Prepared（怪物快照进 ChunkState + 实例回池）
    └─ 离开 D → Serialize + UnloadFull：→ Unloaded（被 Pin 的 Chunk 不卸载）
```

核心类型：

| 类型 | 作用 | 典型路径 |
|---|---|---|
| `MapStreamingSystem` | 流送主控（单例）：A/B/C/D 范围、状态机、任务队列、Pin、坐标换算（支持整体旋转） | `Assets/Scripts/Levels/MapStreaming/MapStreamingSystem.cs` |
| `MonsterSpawner` | 刷怪调度（单例）：监听状态切换，刷怪/AI 激活/快照回收 | `Assets/Scripts/Levels/MapStreaming/MonsterSpawner.cs` |
| `ChunkTileGenerator` | Tile 生成器（纯数据层）：随机 + 模板融合 | `Assets/Scripts/Levels/MapStreaming/ChunkTileGenerator.cs` |
| `ChunkTemplateAllocator` | 全局模板分配器：协调模板抽取与全局约束（mustGenerate/maxCount/weight） | `Assets/Scripts/Levels/MapStreaming/ChunkTileGenerator.cs`（同文件） |
| `ChunkDef` | 地块模板（SO）：Tile 生成规则 + 刷怪表 | `Assets/Settings/MapStreaming/ChunkDef.asset` |
| `TileVisual` | **地形 prefab 玩法组件**：terrainKind/isWalkable/markerType 等随 prefab 走 | `Assets/Scripts/Levels/MapStreaming/TileVisual.cs` |
| `FixedChunkLayout` | 手摆布局（SO）：默认 8×8，每格一个 prefab，作模板来源 | `Assets/Settings/MapStreaming/Templates/` |
| `ChunkLayoutEditorWindow` | 手摆工具：可视化涂刷 FixedChunkLayout | 菜单 `Kepler/Map/Chunk Layout Editor` |
| `MonsterWaveDef` | 怪物编队（SO）：怪物组成 + 刷新权重 | `Assets/Settings/MapStreaming/MonsterWaveDef.asset` |
| `ChunkStateStore` / `PinRegistry` | 快照库 / Pin 注册表（代码内建，无需配置） | 代码内建 |
| `EdgeIndicatorUI` | 屏幕边缘战斗怪方向提示 | `Assets/Scripts/UI/HUD/EdgeIndicatorUI.cs` |
| `MapDebugHUD` | 流送调试面板 + Chunk 边界轮廓（F2） | `Assets/Scripts/Levels/MapStreaming/MapDebugHUD.cs` |

---

## 1. 架构与关键概念

### 1.1 分层结构

```text
Map（无缝大地图）
  └─ Chunk（8×8 Tile，生成/加载/卸载/存档单位）
       └─ Tile（最小格，每格一个 prefab）
            └─ prefab 上挂 TileVisual 组件（玩法语义）
```

| 概念 | 说明 |
|---|---|
| Chunk | 8×8 Tile 的矩形块，生成与流送的基本单位（Chunk 世界边长 = chunkSize × tileSize = 16m） |
| Tile | 最小格，**每格对应一个 prefab** |
| TileVisual | 地形 prefab 上的玩法组件（可走性/地形类别/触发/标记） |
| ChunkDef | 地块模板（SO）：决定一块 Chunk 如何生成、刷什么怪 |
| 生成器 | `ChunkTileGenerator`（纯数据层，输出 TileData 网格） |
| 模板分配器 | `ChunkTemplateAllocator`（全局协调「随机 ↔ 模板」与模板约束） |

**核心原则**：玩法语义（可行走、地形类别、触发、刷怪点等）**全部配置在 prefab 的 `TileVisual` 组件上**，ChunkDef 池直接拖 prefab——逻辑与视觉永不脱节，改 prefab 即改一格。

### 1.2 地块三类

| 类别 | 说明 |
|---|---|
| `Normal` | 普通地面：可行走，每 Chunk 边沿与填充的兜底地面 |
| `Trigger` | 可触发地块：岩浆/地刺/传送/事件等触发区，可走性由 `isWalkable` 配置 |
| `Decoration` | 装饰地块：柱子/雕塑等视觉装饰，**不一定阻挡**（可走性由 `isWalkable` 配置），**模型可超出单格**（如跨格柱子、1×2 长装饰） |

### 1.3 生成模式（唯一：随机 + 模板融合）

只有**一种**生成模式：随机生成与预设模板融合。每个 Chunk 生成时：

1. 按 `templateWeight` 比重决定：走模板 or 走随机
2. 走模板 → 从模板库按权重抽取一个布局，整 Chunk = 该模板的样子
3. 走随机 → 程序生成（边沿地面 → 特殊地形 → 道路/花纹/填充）

```
Chunk 生成
  ├─ 按 templateWeight 命中 → 从模板库加权抽取 → 整 Chunk 逐格映射模板布局
  └─ 未命中 → 程序随机生成：
       ① 边沿一圈：铺普通地面（保证 Chunk 间连通，四边恒开）
       ② 特殊地形图案：触发区（岩浆/地刺等）与装饰物整块放置（内部区域、防重叠）
       ③ 道路带：可选，一条横/竖路砖带
       ④ 花纹区块：plazaCount 个 4×4 区块，循环图案
       ⑤ 填充：剩余格用 fillTiles 生长成片
       ⑥ 兜底：仍空者取 normalTiles（每 Tile 非空）
```

### 1.4 模板的全局约束

模板条目带全局约束，由 `ChunkTemplateAllocator` 统一协调（整张地图共享）：

| 约束 | 含义 |
|---|---|
| `mustGenerate` | 是否必须生成：该模板整张地图**至少出现一次**（分配器优先保证） |
| `maxCount` | 最大生成数量：该模板地图内**总数上限**（抽中达到上限后从池中移除；≤0 = 不限制） |
| `weight` | 抽取权重（≤0 且非 mustGenerate 不参与抽取） |

> **确定性**：模板分配在 Chunk 首次生成时确定并缓存（coord → 布局），Chunk 离开范围回收后重新生成时直接复用，不重复计数——保证「同 seed 同结果」与全局计数不冲突。

### 1.5 确定性

同 `worldSeed` + Chunk 坐标 → 同结果，生成顺序无关。随机源仅 `System.Random(ChunkSeed(coord, salt))`，禁用 `UnityEngine.Random` 与时间。

```
ChunkSeed(coord, salt) = worldSeed ^ (coord.x * 73856093) ^ (coord.y * 19349663) ^ (salt * 83492791)
```

> 注意：模板分配器直接使用 `ChunkSeed` 作为随机种子，**不要再对它做二次哈希**（与 `ChunkSeed` 同构的哈希会让 coord 项抵消，导致所有 Chunk 同种子、全变同一个模板）。

### 1.6 Chunk 状态机

```
None →(进 C: Prepare)→ Prepared →(进 B: Instantiate)→ Dormant →(进 A: Activate)→ Active
                                              ↑                        ↓
                                              └────(离开 A: Pause)──────┘
Prepared →(离开 D: Serialize→UnloadFull)→ Unloaded
Dormant →(离开 B: UnloadScene)→ Prepared
```

- A/B 间往返不卸载；被 Pin 的 Chunk 离开 D 不卸载，解 Pin 且在 D 外时补发回收。

### 1.7 刷怪全流程

| 状态切换 | 行为 |
|---|---|
| Prepared→Dormant（进 B） | 无快照 → 收集刷怪点 → 4 项校验 → 权重抽波 → 刷出（AI 休眠）；有快照 → 恢复不重摇 |
| Dormant→Active（进 A） | `aiActiveOverride = true`，AI 激活追击 |
| Active→Dormant（离开 A） | AI 休眠（附身怪跳过） |
| Dormant→Prepared/Unloaded（离开 B/D） | 快照进 ChunkState 再回池 |

- **重入不重摇**：`spawnedWaveIds` 抽中即登记，重进判"摇过"而非"还有怪"——全灭 Chunk 重入不重刷。
- **配额与回收**：全场配额 30，满时不刷；脱战且距玩家 > B、视野外 → 快照写回后回池；近玩家但 AI 休眠的怪强制激活。

---

## 2. 配置原则

1. **ChunkDef 是唯一入口**：一块 Chunk 的"长什么样 + 刷什么怪"全部由 `ChunkDef` 决定，`MapStreamingSystem.defaultChunkDef` 引用它（WorldPlan 接入前所有 Chunk 共用同一模板）。
2. **玩法语义随 prefab 走（核心原则）**：Tile 的可走性、地形类别、触发、刷怪标记等**全部配置在 prefab 的 `TileVisual` 组件上**，ChunkDef 池直接拖 prefab。
3. **每个 Tile 一个 prefab**：Tile prefab 本身即"一格大小"的标准方块（1×1 轴对齐、底面 y=0），普通/触发地块由代码按 `tileSize` 缩放铺满一格；**装饰地块保留 prefab 原始尺寸（允许超格）**。
4. **池为空 = 该类不生成**：`triggerTiles`/`decorationTiles` 任一为空，对应 patterns 不参与抽取；`normalTiles` 为空会告警（每 Tile 非空是硬要求）。
5. **波次与地块解耦**：`ChunkDef.waveTable` 挂 `MonsterWaveDef`；`MonsterWaveDef.id` 是去重键（重入不重摇），务必唯一非空。
6. **四级半径不变量 A ≤ B ≤ C < D**：D 必须严格大于 C（卸载滞后区）；当前定为 **25 / 50 / 60 / 80 m**。
7. **确定性**：同 `worldSeed` + Chunk 坐标 → 同结果。

---

## 3. 配置资产速查

创建菜单：`Assets → Create → Kepler/Map →`（Chunk Def / Monster Wave / Fixed Chunk Layout）。

### 3.1 ChunkDef（地块模板，核心）

#### 模板库

| 字段 | 默认 | 含义 |
|---|---|---|
| `templateEntries` | 空 | 模板条目列表（`ChunkTemplateEntry`） |
| `templateWeight` | 0 | 走模板（而非随机）的比重；0 = 全随机，1 = 全模板 |

每个 `ChunkTemplateEntry`：

| 字段 | 默认 | 含义 |
|---|---|---|
| `layout` | 空 | 手摆布局资产（`FixedChunkLayout`，每格一个 prefab） |
| `mustGenerate` | false | 是否必须生成（整张地图至少一次） |
| `maxCount` | 1 | 最大生成数量（≤0 = 不限制） |
| `weight` | 1 | 抽取权重（≤0 且非必生成不参与） |

#### 地形 prefab 池（每项 = 一个 Tile prefab，挂 TileVisual）

| 字段 | 含义 |
|---|---|
| `normalTiles` | 普通地面候选（可行走）。边沿与填充均从此取；**为空告警** |
| `triggerTiles` | 可触发地块候选（岩浆/地刺/传送/事件等）。玩法由 prefab 的 terrainKind 区分 |
| `decorationTiles` | 装饰地块候选（柱子/雕塑等，**不一定阻挡**）。可走性由 prefab 的 `isWalkable` 配置 |

#### 形状图案（特殊地形整块放置）

| 字段 | 默认 | 含义 |
|---|---|---|
| `triggerPatterns` | Line3(1) + Single(0.5) | 可触发地块图案 |
| `decorationPatterns` | Single(1) | 装饰地块图案 |
| `minPatternsPerChunk` | 1 | 每 Chunk 至少放置的图案数 |
| `maxPatternsPerChunk` | 4 | 图案数上限（放不下则少放） |

> **图案形状**：`Single`=1×1、`Line2`=1×2、`Line3`=1×3、`Square2`=2×2、`LShape`=L 形 3 格。随机朝向，覆盖格须在内部且未分配（防重叠）。

#### 地面视觉（Normal 结构化排布）

| 字段 | 默认 | 含义 |
|---|---|---|
| `roadTiles` | 空 | 道路地砖（空 = 不生成道路带） |
| `plazaTiles` | 空 | 花纹区块地砖组（4×4 循环图案） |
| `fillTiles` | 空 | 普通填充地砖（**为空回退 normalTiles**） |
| `roadChance` | 0.5 | 出道路带的概率 |
| `plazaCount` | 1 | 花纹区块个数 |
| `groundSpreadChance` | 0.92 | 填充邻接继承概率（高=成片不碎斑） |

#### 其它

| 字段 | 含义 |
|---|---|
| `id` | 配置唯一 id（调试/WorldPlan 引用） |
| `waveTable` | 刷怪表（MonsterWaveDef 列表，按 spawnWeight 抽取） |
| `preferredNeighbors` | 偏好相邻 Chunk 模板（宏观区域排布预留，暂未接入） |
| `isSafeFallback` | 是否安全预制（出入口校验失败时替换用） |

### 3.2 TileVisual（地形 prefab 玩法组件）

**手动挂载**在地形 prefab 根节点上即可（无需额外工具）。

| 字段 | 默认 | 含义 |
|---|---|---|
| `terrainKind` | Normal | Normal/Trigger/Decoration（普通地面/可触发地块/装饰地块） |
| `isWalkable` | true | 该格是否可行走（**装饰地块可配置**：可走装饰物/阻挡性装饰物） |
| `markerType` | None | 玩法标记：`SpawnPoint`（刷怪点）/`Entrance`/`EventTrigger`/`Teleport` |
| `displayName` | 空 | 显示名（调试/手摆工具） |

> **触发/伤害配置**：岩浆/地刺等触发区当前为**纯视觉**（`terrainKind=Trigger`），伤害逻辑待接入；届时在 TileVisual 上按需新增配置字段（如触发模式/伤害效果引用），**不要**用 `isHazard` 布尔（已移除，避免单一布尔无法表达"燃烧/瞬伤/传送"等不同行为）。

> **装饰地块跨格**：Decoration 类 prefab 不强制缩放到 1×1，保留 prefab 原始 Scale（允许模型自然超出单格边界），视觉层仅做 pivot 校正（中心对齐格中心、底面对齐 y=0）。阻挡性装饰物（`isWalkable=false`）实例化时若无碰撞体会自动补 BoxCollider。

> **刷怪点**：TileVisual.markerType = `SpawnPoint` 的可走 Tile 被刷怪系统识别为刷怪点；无标记点则回退 Chunk 中心附近随机可走格。

### 3.3 MonsterWaveDef（怪物编队）

| 字段 | 默认 | 含义 |
|---|---|---|
| `id` | 空 | **去重键**（重入不重摇），务必唯一非空 |
| `spawnWeight` | 1 | 刷新权重，必须 > 0 |
| `monsters` | 空 | 怪物组成（prefab + count 列表） |
| `baseCount` / `threatValue` / `bodySupply` | 4/1/1 | 字段就绪但未参与计算 |
| `statMult` | 1/1/1 | 属性倍率。**故意不应用**（池复用会累积放大） |

> 怪物 prefab 必须挂 `MonsterActor`，刷出时查 `GetComponentInChildren<MonsterActor>`，查不到直接销毁。

### 3.4 FixedChunkLayout（手摆布局）

策划手摆的 N×N 网格（**默认 8×8**，Inspector 改 `size` 即变边长），**每格直接存 prefab 引用**（空格 = 可走普通地面）。用 `Kepler → Map → Chunk Layout Editor` 可视化涂刷：

- **左侧刷子面板**：列出 `Assets/Prefabs/MapModules/` 下全部 prefab（挂 `TileVisual` 的正常显示；未挂的灰色标注），按类别着色，**资产增删自动刷新**（AssetPostprocessor）；
- **右侧网格**：点击/拖拽涂刷；顶部/左侧有坐标轴标签；空格为棋盘格底色；悬停任意格在底部信息面板显示 坐标/prefab/TileVisual 全字段；
- **可缩放**：工具栏滑块/＋−按钮放大缩小棋盘（24~72px/格）；
- **颜色**：同类地砖自动按名称数字变体散列上色（Brick01~06 颜色各异），不同类用 kind 色（绿/橙/红）；
- 保存为 `FixedChunkLayout` 资产，作为模板条目 `layout` 的来源。

> 布局边长应与 `MapStreamingSystem.chunkSize` 一致（生成器告警但不中断；不一致时只铺左上 size×size、其余空）。

### 3.5 MapStreamingSystem（流送主控，必挂）

| 字段 | 默认 | 含义 |
|---|---|---|
| `playerOverride` | 空 | 手动指定玩家 Transform；空则自动查找 `PlayerController.Instance` → tag "Player" → 系统自身位置 |
| `radiusA` / `radiusB` / `radiusC` / `radiusD` | 25/50/60/80 | 四级范围（米），**D 必须 > C** |
| `chunkSize` | 8 | Chunk 边长（Tile 数） |
| `tileSize` | 2 | 每 Tile 世界尺寸（米）。Chunk 世界边长 = 16m |
| `worldSeed` | 12345 | 单局种子：`ChunkSeed = Hash(coord, worldSeed)`，生成顺序无关 |
| `defaultChunkDef` | 空 | **默认 Chunk 模板**（WorldPlan 接入前所有 Chunk 共用） |
| `tickInterval` | 0.2 | 范围集合重算间隔（秒） |
| `activatePerFrame` 等 7 项 | 2/5/2/4/2/2/4 | 各 JobKind 每帧个数预算 |
| `timeBudgetMs` | 4 | 时间片兜底（毫秒）：超时立即停，剩余下帧；0 = 关闭 |
| `showGizmos` | true | Scene 视图画 A/B/C/D 范围圆 + Chunk 状态色块 |
| `logStateChanges` | true | Chunk 状态切换 Debug.Log |

> **旋转支持**：`MapStreamingSystem` 内部统一用"地图本地坐标"，本物体整体平移/旋转（如旋转 45°）地图会跟随；**不支持非均匀缩放**（Awake 有告警）。

### 3.6 MonsterSpawner（刷怪调度，必挂）

| 字段 | 默认 | 含义 |
|---|---|---|
| `maxCombatMonsters` | 30 | 全场配额（Active + Dormant）。满时不刷、已刷不回收 |
| `maxWavesPerChunk` | 2 | 单 Chunk 刷怪波次上限 |
| `scatterRadius` | 3 | 波内怪物散布半径（米） |
| `minSpawnDistanceToPlayer` / `maxSpawnDistanceToPlayer` | 20 / 50 | 刷新点到玩家距离区间（对齐 A/B 半径） |
| `edgeIndicatorMaxCombat` | 10 | 战斗怪 ≤ 该数量才产生边缘提示 |
| `edgeOutOfViewSeconds` | 2 | 持续视野外超该时长才纳入提示 |
| `upkeepInterval` | 0.25 | 低频维护间隔 |
| `showGizmos` / `logSpawns` / `showDebugHud` | true | 怪物圆点 / 刷怪日志 / 右上角 HUD |

> **刷怪 4 项校验**：① 距离 ∈ [min, max]；② 刷怪点在玩家相机视锥外；③ Tile 可走；④ 连通（v1 弱化：仅复用 isWalkable）。

### 3.7 EdgeIndicatorUI（边缘方向提示，可选）

| 字段 | 默认 | 含义 |
|---|---|---|
| `arrowSprite` | 空 | 箭头贴图；空用程序化三角图元 |
| `edgePadding` / `indicatorSize` | 48 / 36 | 边缘距离 / 图标边长（像素） |
| `arrowColor` / `showDistance` | 威胁红 / true | 箭头颜色 / 是否显示距离 |

挂任意物体即生效（运行时自建 ScreenSpaceOverlay Canvas）。

### 3.8 MapDebugHUD（调试面板，可选）

| 字段 | 默认 | 含义 |
|---|---|---|
| `showHud` | false | 是否显示调试 HUD |
| `toggleKey` | F2 | 显示切换快捷键（**同时切换 Chunk 边界轮廓**） |
| `refreshInterval` | 0.25 | 统计刷新间隔 |
| `showChunkBorders` | true | HUD 开启时绘制 Chunk 边界线（按状态着色） |
| `borderHeight` | 0.05 | 边界线世界高度（防 z-fighting） |

场景无实例时运行时自动创建，无需预摆。

---

## 4. 标准配置流程（从零到跑通）

### 步骤 1：制作地形 prefab

**手动流程**（无需专用工具）：

1. 准备一个 1×1 轴对齐的面片（底面 y=0、XZ 中心在原点、法线朝上）
2. 在 prefab 根节点上**手动添加 `TileVisual` 组件**
3. 填写玩法语义（terrainKind / isWalkable / markerType）
4. 按需添加碰撞体（实体地形用 BoxCollider，触发区用 Trigger；阻挡性装饰物可不加，代码会自动补）

已做好的地形 prefab：`Assets/Prefabs/MapModules/`（`Normal/` 地砖面片 Brick01~06 + Base_Ground_Tile，`Trigger/` 岩浆 lava、地刺 Spike_Trap，`Decoration/` 柱子 Pillar、雕塑 Statue）。

> **规范**：Tile prefab 应是 1×1 轴对齐、底面 y=0 的标准面片。美术 fbx 常带旋转面，务必手动规范化，否则会出现"浮空/斜纹理/立方体"等问题。`TileVisual.OnValidate` 会在编辑期自检并提示不规范之处。

### 步骤 2：创建 ChunkDef 并接池

1. `Assets → Create → Kepler/Map → Chunk Def`
2. 拖 prefab 到对应池：`normalTiles`（地面）、`triggerTiles`（可触发）、`decorationTiles`（装饰）
3. 配形状图案（`triggerPatterns` / `decorationPatterns`）+ 地面视觉（`fillTiles` 复用 normalTiles、`plazaTiles`、`roadTiles`）
4. 配模板库（可选）：拖手摆布局到 `templateEntries`，设 `templateWeight` 比重
5. `waveTable` 挂 MonsterWaveDef

### 步骤 3：手摆模板（可选）

用 `Kepler → Map → Chunk Layout Editor`：

- 左侧：prefab 刷子（挂 TileVisual 的 prefab，按类别着色）
- 右侧：8×8 网格，点击/拖拽涂刷
- 保存为 `FixedChunkLayout`，拖进 ChunkDef 的 `templateEntries` 条目作为 `layout`

### 步骤 4：场景搭建

1. 空物体 `StreamingTest` 挂 `MapStreamingSystem` + `MonsterSpawner`（+ `EdgeIndicatorUI`）
2. `MapStreamingSystem.defaultChunkDef` 拖入 ChunkDef；半径默认 25/50/60/80
3. 玩家：自动识别，或 `playerOverride` 指向 Main Camera 后飞行调试

### 步骤 5：运行验证

见 §6。

---

## 5. 运行时行为语义

### 5.1 Chunk 状态机

见 §1.6。

### 5.2 刷怪全流程

见 §1.7。

### 5.3 重入不重摇

`spawnedWaveIds` 抽中即登记，重进判"摇过"而非"还有怪"——全灭 Chunk 重入不重刷。

### 5.4 配额与回收

- 全场配额 30：满时不刷；配额中断的波次本局不补。
- 脱战回收：脱战且距玩家 > B、视野外 → 快照写回后回池。
- 孤儿怪兜底：近玩家但 AI 休眠的怪强制激活。

---

## 6. 调试验证

### 6.0 测试环境与入口（推荐在 EnemyAiTest 的 StreamingTest 下测）

**推荐场景**：`Assets/Scenes/EnemyAiTest.unity`（同时验证流送 + 刷怪 + AI）。

**测试对象**：场景中名为 **`StreamingTest`** 的空物体（挂 `MapStreamingSystem` + `MonsterSpawner`）。

**跑通步骤**：

1. 打开 `EnemyAiTest` 场景；
2. 选中 `StreamingTest`，确认 Inspector 里：
   - `MapStreamingSystem.defaultChunkDef` 已拖入 `ChunkDef.asset`；
   - `radiusA/B/C/D` = 25 / 50 / 60 / 80；
   - `chunkSize` = 8、`tileSize` = 2（Chunk 世界边长 16m）；
   - `playerOverride` 指向玩家（Main Camera 或飞行调试物体）；
3. 点 **Play**；
4. **Scene 视图**：立即看到以玩家为圆心的 A/B/C/D 范围圈 + Chunk 状态色块（Gizmos，§6.1 图例）；
5. 按 **F2**：打开调试 HUD（左上）并显示 Chunk 边界轮廓线；
6. **移动玩家/相机**（A/D/W/S 或直接拖动 Main Camera）：观察周围 Chunk 依次 Prepared（黄）→ Dormant（蓝，刷怪）→ Active（绿，AI 激活追击）；离开后反序休眠/卸载；
7. **反复进出同一区域**：验证"重入不重摇"（全灭 Chunk 重入不重刷）、确定性（同 seed 同结果）。

**推荐测试点**：
- 看**区域风格**：不同 Chunk 呈现模板（8×8 手摆布局）或随机生成（触发区/道路/花纹/填充）的差异；
- 看**刷怪**：B 带刷出休眠怪（蓝点，不动正常），进 A 后激活追击（绿点），脱战回池；
- 看**模板分配**：`ChunkDef.templateEntries` 里的模板按 `weight`/`maxCount`/`mustGenerate` 全局协调。

> 也可用 `Assets/Scenes/CombatTest.unity` 做纯战斗测试（无流送侧重）。

### 6.1 Gizmos 图例

| 颜色/形状 | 含义 |
|---|---|
| 绿/蓝/黄/红圈 | A/B/C/D 范围（25/50/60/80m） |
| 绿/蓝/黄/灰色块 | Chunk 状态 Active/Dormant/Prepared/Unloaded |
| 紫线框 | 被 Pin 的 Chunk |
| 绿/蓝圆点 | AI 激活/休眠的怪（MonsterSpawner） |
| F2 边界线（绿/蓝/黄/灰） | Chunk 边界轮廓，按状态着色（MapDebugHUD） |

### 6.2 验证清单

| 动作 | 预期现象 |
|---|---|
| Play 后静止 | 出生区 Chunk 依次 Prepared（黄）→Dormant（蓝，刷怪）→Active（绿） |
| 前进 | 新 Chunk 依次点亮；B 带刷怪（蓝点休眠，不动是正常的）；进 A 激活追击 |
| 离开 A/B | 怪休眠 → 快照回收消失 |
| 再进 B | 快照恢复，不重摇 |
| 按 F2 | 左上调试面板 + Chunk 边界轮廓线 |
| 怪追出视野 | 屏幕边缘红色箭头 + 距离 |

### 6.3 不刷怪排查

1. `waveTable` 空 或 `spawnWeight` 全 0
2. `刷怪点距离校验失败：要求 [20, 50]` → 玩家太近/太远（无玩家时所有点 <20 被拒）
3. `刷怪点在玩家视野内` → 换方向
4. `全场怪物达上限（30）`
5. `prefab 无 MonsterActor` → 拖错 prefab

---

## 7. 常见坑

| 现象 | 原因 | 处理 |
|---|---|---|
| 地形 prefab 没挂 TileVisual | 玩法语义兜底默认值（Normal/可走） | 手动挂 TileVisual 并填语义 |
| 池里 prefab 的 terrainKind 与池不匹配 | OnValidate 告警，生成时 kind 以池为准 | 检查 prefab 的 terrainKind |
| 装饰物调整 Scale 不生效 | 旧版代码覆盖 localScale | 已修复：装饰地块保留原始 Scale |
| 模板全部变成同一个 | 模板种子被二次哈希抵消（coord 项抵消） | 已修复：分配器直接用 ChunkSeed |
| Tile 是立方体 | 用了 Cube 网格的 prefab | 规范化成 1×1 面片 |
| 地面碎斑 | groundSpreadChance 过低 | 调高（如 0.92） |
| 忘了配 `MonsterWaveDef.id` | 去重键空会回退资产名，改名即重摇 | 填唯一 id |
| radiusA 与刷怪距离不匹配 | A 圈内无怪 | 调大 radiusA 超过 minSpawnDistance(20) |
| 怪物刷出来不动 | B 带内 AI 休眠，进 A 才激活 | 正常现象 |
| 改了 ChunkDef 不生效 | `defaultChunkDef` 引用丢失/没拖 | 检查引用 + Console 日志 |
| 重复挂载单例 | 多挂会销毁后者 | 只挂一个 |
| 模板始终不出现 | templateWeight=0 / weight≤0 / maxCount 已达上限 / mustGenerate 未勾选 | 检查模板条目配置 |

---

## 8. 代码接入点（二次开发参考）

| API | 说明 |
|---|---|
| `MapStreamingSystem.OnChunkStateChanged(coord, old, new)` | Chunk 状态切换事件 |
| `MapStreamingSystem.OnChunkEnteredA(coord)` | 进入 A 事件 |
| `MapStreamingSystem.Pins.Pin/Unpin(coord, reason)` | Pin/解 Pin |
| `MapStreamingSystem.States` | ChunkState 快照库（ToJson/FromJson 预留落盘） |
| `ChunkTileGenerator.Generate(chunk, def, seed, allocator)` | 生成入口（随机+模板融合，allocator 可空） |
| `MonsterSpawner.OnMonsterSpawned/OnMonsterRecycled` | 刷出/回收事件 |
| `MonsterSpawner.OffscreenCombatMonsters` | 视野外战斗怪列表 |
| `MonsterActor.aiActiveOverride` / `ApplyStreamSnapshot` | 流送休眠开关 / 快照恢复 |

---

## 9. 当前实际配置与遗留 TODO

### 9.1 当前配置

| 资产 | 配置 |
|---|---|
| `ChunkDef.asset` | normalTiles=Brick01~06 等 6 地砖；triggerTiles=lava+Spike_Trap；decorationTiles=Pillar+Statue；fillTiles/plazaTiles=地砖；`templateEntries` 挂 3 个 8×8 模板（`Template_All_Brick01` / `Template_Ring3` / `Template_VolcanoCenter`），`templateWeight` 决定模板与随机比重 |
| `MonsterWaveDef.asset` | id=`wave_pride_test`，spawnWeight=0.5，monsters=[pride_new ×2, sloth_new ×2] |
| `Regions/`（预留） | 4 个 RegionDef 供 WorldPlan（未接入场景） |

> 6 个地砖 prefab（Brick01~06）目前可能材质雷同，若视觉区分不大需美术配不同贴图。

### 9.2 遗留 TODO

| 项 | 状态 |
|---|---|
| 触发地块伤害逻辑 | 未接入（当前 `terrainKind=Trigger` 纯视觉；接入时在 TileVisual 上新增触发配置字段，如模式/伤害效果引用） |
| WorldPlan 宏观区域（噪声 + 主题 + 邻接加权） | 未接入（`Regions/` 已备 4 个 RegionDef） |
| 边界签名（相邻 Chunk 边沿开口对齐） | 未接入，当前边沿恒可走 + 四边恒开 |
| 跨 Chunk 连续道路 | 未做（当前 Chunk 内随机带） |
| 功能块（第五类地形/固定刷怪点） | 未做（刷怪点现经 markerType=SpawnPoint 实现） |
| NavMesh 增量烘焙 | 暂缓（怪物移动用 transform 直移 + SphereCast） |
| `statMult` / `baseCount` / `threatValue` / `bodySupply` | 字段就绪未参与计算 |
| 尸体/奖励/事件恢复 + 落盘序列化 | 快照字段占位，恢复未实现 |
| `preferredNeighbors` / `isSafeFallback` | 字段就绪，宏观排布接入后生效 |
| 模板分配跨 WorldPlan | 全局分配器目前基于 defaultChunkDef，宏观接入后需统一协调 |
