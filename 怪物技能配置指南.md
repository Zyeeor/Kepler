# 怪物技能配置指南

本文说明如何在不改动核心代码的前提下，为新增怪物配置技能、卡牌构筑、Gameplay Effect、特效和命中盒调试。

## 1. 配置原则

- 每个怪物技能都是一个 `EnemyAbility` 组件，挂在怪物 Prefab 或其子物体上。
- 普攻使用 `BasicAttack`，Q 技能使用 `Skill`，Space 技能使用 `Mobility`。
- 复杂行为由对应 Ability 脚本实现；数值、特效、Effect、卡牌标签均在 Inspector 配置。
- 不使用技能显示名称匹配卡牌。必须使用稳定的 `abilityTags` 与 `CardData.targetAbilityTags`。
- 新技能的命中伤害必须调用 `DealDamageTo` 或 `DealDamageToPlayer`，以确保吸血、伤害倍率和附加 Effect 生效。

## 2. 怪物与能力的基础配置

### 2.1 能力组件

在怪物 Prefab 上添加所需的 `EnemyAbility` 子类，并设置：

| 字段 | 用途 | 配置建议 |
|---|---|---|
| `abilityName` | UI 显示名称 | 只用于展示，不用于卡牌定位 |
| `type` | 技能输入类型 | 普攻/技能/位移分别设为 `BasicAttack`、`Skill`、`Mobility` |
| `damage` | 基础伤害 | 所有伤害倍率以此为基准 |
| `cooldown` | 冷却秒数 | 受攻击速度修正 |
| `abilityTags` | 稳定能力标签 | 例如 `Ability.Monster.Pride.SwordQi` |
| `upgrades` | 可解锁升级 ID | 每个卡牌 `effectId` 在这里建立一个 Slot |
| `appliedEffectTags` | 命中附加 Effect | 配置 `Effect.Control.Stunned`、`Effect.Debuff.Burning` 等 |
| `activationEffect` | 施法期间自身状态 | 前摇、不可移动、无敌等状态 |
| `vfxPrefab` | 技能释放特效 | 只用于技能释放瞬间或短时表现 |
| `drawHitboxes` | 单技能命中盒调试 | 仅开发调试时开启 |

### 2.2 推荐标签命名

使用以下格式，避免同名技能、换名或复制 Prefab 导致卡牌失效：

```text
Ability.Monster.<Sin>.<Skill>
Ability.Monster.Pride.SwordQi
Ability.Monster.Sloth.Drone
Ability.Monster.Gluttony.Devour
Ability.Monster.Envy.ChainLightning
Ability.Monster.Greed.ReturningBlade
```

状态和 Effect 使用现有分层 Tag：

```text
State.Control.Stunned
State.Control.Rooted
State.Defense.Untargetable
State.Defense.Flying
Effect.Control.Stunned
Effect.Debuff.Burning
```

## 3. Gameplay Effect 配置

`GameplayEffectDefinition` 是目标身上的状态定义。创建 Effect Asset 后，将其登记到 `GameplayTagCatalog`，并把该 Catalog 赋给场景中的 `CardManager`。

### 3.1 常用 Effect

| Effect | effectTag | grantedTags | 时长 | 表现 |
|---|---|---|---:|---|
| 眩晕 | `Effect.Control.Stunned` | `State.Control.Stunned` | 2 秒 | `activeVfxPrefab` 配紫色眩晕环 |
| 定身 | `Effect.Control.Rooted` | `State.Control.Rooted` | 按技能配置 | 持续束缚特效 |
| 不可选中 | `Effect.Defense.Untargetable` | `State.Defense.Untargetable` | 穿梭斩持续时间 | 角色半透明/残影 |
| 飞行 | `Effect.Defense.Flying` | `State.Defense.Flying` | 飞行持续时间 | 翅膀与升空特效 |
| 小猫形态 | `Effect.Form.SmallCat` | `State.Form.SmallCat` | 3 秒 | 缩小模型与黑白特效 |

### 3.2 表现字段

- `activeVfxPrefab`：Effect 存在期间挂在目标身上。
- `applyVfxPrefab`：Effect 成功施加时播放一次。
- `expireVfxPrefab`：Effect 结束或移除时播放一次。
- `oneShotVfxDuration`：一次性 VFX 的销毁时间。
- `periodicInterval`：周期回调间隔。周期伤害、治疗或脉冲逻辑由对应技能/组件订阅 `CombatAbilityComponent.OnEffectPeriodic` 后执行。

注意：Effect 负责“状态”和“持续表现”；弹体、挥砍、爆炸等技能过程表现仍由 Ability 的 `vfxPrefab` 或专用 VFX 字段负责。

## 4. 卡牌 Build 配置

每张卡牌对应一个 `CardData`。选择卡牌后，`CardManager` 会永久解锁本局对应 `effectId`，并把升级同步到现存与后续生成的怪物。

### 4.1 卡牌字段

| 字段 | 用途 |
|---|---|
| `cardName`、`image`、`description` | UI 展示 |
| `effectId` | 升级唯一 ID，例如 `Pride.BlinkCount` |
| `targetAbilityTags` | 目标能力标签；必须优先配置 |
| `grantedEffectTags` | 该能力命中时附加的 Gameplay Effect |
| `abilityParameters` | 数值覆盖参数 |

### 4.2 数值参数约定

`abilityParameters.key` 由目标 Ability 读取。当前原型支持：

| key | 含义 |
|---|---|
| `ChargeDistance` | 突进/弹射距离 |
| `BlinkCount` | 穿梭斩次数 |
| `SummonCount` | 召唤物数量 |
| `TransformDuration` | 形态持续时间 |
| `FlightDuration` | 飞行持续时间 |
| `ExecuteThreshold` | 斩杀生命比例 |
| `BladeCount` | 环绕飞刀数量 |

示例：傲慢“穿梭 +2 次”卡牌配置如下。

```text
effectId: Pride.BlinkCountPlusTwo
targetAbilityTags: Ability.Monster.Pride.BlinkChain
abilityParameters:
  - key: BlinkCount
    value: 6
```

## 5. 新怪物技能原型配置

### 5.1 通用原型：EnemyAbility_PrototypeMonsterSkill

该组件用于实现需求表中尚未具备专用脚本的技能。根据 `mode` 选择机制：

| mode | 对应需求 | 核心字段 |
|---|---|---|
| `ChargeStrike` | 傲慢蓄力突进、怠惰弹射 | `range`、`radius`、`speed`、`damageMultiplier` |
| `BlinkChain` | 傲慢穿梭斩 | `count`、`blinkInterval`、`selfEffect` |
| `Drone` | 怠惰木灵无人机 | `spawnedPrefab`、`duration`、`summonAttackInterval`、`summonAttackRange` |
| `Transform` | 暴食小猫形态 | `selfEffect`、`duration` |
| `ExecuteArc` | 暴食吞噬/斩杀 | `range`、`damageMultiplier`、`ExecuteThreshold` |
| `Flight` | 嫉妒飞行 | `selfEffect`、`duration` |
| `Grapple` | 愤怒自我钩拉位移 | `range`、`speed` |
| `Summon` | 色欲召唤小怪 | `spawnedPrefab`、`count`、`duration` |

`targetEffect` 用于命中目标时直接施加 Effect，例如链雷的眩晕。

### 5.2 贪婪：EnemyAbility_ReturningBladeOrbit

为贪婪的技能添加该组件，配置：

| 字段 | 建议 |
|---|---|
| `bladePrefab` | 念力飞刀/鬼手投射物表现 |
| `bladeCount` | 默认 3；卡牌可用 `BladeCount` 调整 |
| `orbitRadius` | 环绕半径 |
| `orbitDuration` | 默认 5 秒 |
| `hitRadius` | 单把飞刀碰撞范围 |
| `hitInterval` | 同目标重复受击间隔 |
| `maxSplitCount` | 后续分裂升级的上限 |
| `returnSpeed` | 飞刀返航速度 |

飞刀击杀分裂、内外双环等视觉与弹道扩展应保持在该组件中实现；卡牌只提供数量、速度、半径等数值。

## 6. 各怪物落地映射

### 傲慢：终刃绝影

- 普攻：沿用 `EnemyAbility_SwordQi`；卡牌配置穿透、散射、远程化。
- 位移：`ChargeStrike`，升级配置 `ChargeDistance`。
- Q：`BlinkChain`，配置不可选中 `selfEffect`；卡牌配置 `BlinkCount`。
- 气刃：在每次穿梭后由专用剑气能力释放，朝本次穿梭方向发射。

### 怠惰：机械之灵

- 位移：`ChargeStrike`，前摇用 `activationEffect`，落点伤害用 `radius`。
- 普攻：沿用 `EnemyAbility_ChargeShot`；散射与爆炸升级沿用既有 Upgrade Slot。
- Q：`Drone`，无人机 Prefab 配到 `spawnedPrefab`；无人机死亡爆炸由其 Prefab 上的能力/碰撞组件配置。

### 暴食：魔猫

- 位移：`Transform`，配置小猫形态 Effect（移速倍率、减伤或消耗折扣由后续专用 Modifier 支持）。
- 普攻：地下巨口使用范围 Ability，第三段通过 Ability 内连击计数触发额外范围攻击。
- Q：`ExecuteArc`，`ExecuteThreshold = 0.2`；“吞噬后释放敌人技能”需要为可吞噬能力添加显式白名单。

### 嫉妒：激光异形

- 位移：`Flight`，配置飞行/不可选中 Effect。
- 普攻：沿用 `EnemyAbility_Laser`；多目标与增粗表现由已有升级逻辑和 VFX 参数配置。
- Q：沿用 `EnemyAbility_ChainLightning`，目标 Effect 配 `Effect.Control.Stunned`。

### 愤怒：链狱冥兽

- 位移：`Grapple`，朝鼠标方向或当前朝向自我位移。
- Q：沿用链拉技能；范围吸附和龙卷风表现应配置在专用 VFX 与范围能力上。

### 贪婪：魔鬼手

- 普攻：沿用 `EnemyAbility_TKDagger`，用于单把追踪飞刀。
- Q：使用 `EnemyAbility_ReturningBladeOrbit`，配置环绕、持续伤害、返航。

### 色欲：灵念师

- Q：`Summon`，配置召唤物 Prefab、数量和存活时间。
- 位移与普攻尚未在需求表定义，暂不配置具体行为。

## 7. 命中盒调试

1. 创建一个空对象，例如 `CombatDebug`。
2. 添加 `CombatHitboxDebugSettings`。
3. 勾选 `enableHitboxDebug`。
4. 在需要查看的技能组件上勾选 `drawHitboxes`。
5. 进入 Play Mode，在 Scene View 中查看 Debug 线框。

当前可视化覆盖共享球体、盒体、扇形、路径胶囊，以及已接入的剑气、蓄力射击、飞轮、飞刀、地雷和新增原型能力。未接入的历史技能需要在其 Physics 查询前调用 `CombatHitboxDebug` 对应绘制方法后才会显示。

## 8. 配置验收清单

- 每个 Ability 都有唯一 `abilityTags`。
- 每张卡都填写 `targetAbilityTags`，不依赖 `abilityPrefab` 名称回退。
- 每个 `grantedEffectTags` 都已在 `GameplayTagCatalog` 中登记到 Effect Asset。
- 控制类 Effect 配置了正确的 `grantedTags`，并验证目标的免疫规则。
- 所有持续状态配置 `activeVfxPrefab`，瞬时反馈配置 `applyVfxPrefab`。
- 技能 VFX、投射物 Prefab、召唤物 Prefab 均已在 Inspector 赋值。
- Play Mode 下启用命中盒调试，逐项确认范围、角度、移动路径和命中层。
