# 怪物技能配置指南

本文说明如何基于当前战斗框架（Ability / Effect / Tag / Card）为怪物配置技能。  
**以已落地的「傲慢-终刃绝影」`pride_new` 为完整范例**；其他七宗罪按同一流程复制即可。

需求来源：`项目全量表 - 怪物设计（新）.csv`。  
参考实现：`Assets/Prefabs/Monster/pride_new.prefab`。  
快速测试：§10（`CombatTest` + `MonsterPossessionCheat` 数字键）。

---

## 0. 框架速览

```text
玩家输入（左键 / Q / Space）
    ↓
MonsterActor 按 AbilityType 触发对应 EnemyAbility
    ↓
EnemyAbility.Trigger()
  ├─ activationEffect（可选，施法期间自身状态）
  ├─ OnTrigger() 业务协程
  └─ DealDamageTo / ApplyEffect
        ├─ 扣血、吸血、伤害倍率
        └─ appliedEffectTags → CardManager → GameplayTagCatalog → GameplayEffectDefinition
              ├─ grantedTags（如 State.Defense.Untargetable）
              └─ hitVfx / activeVfx / weaponVfx
```

核心类型：

| 类型 | 作用 | 典型路径 |
|---|---|---|
| `EnemyAbility` | 技能行为与数值 | `Assets/Scripts/Combat/Abilities/` |
| `CombatAbilityComponent` | Tag / Effect 运行时容器 | 挂在怪物根上 |
| `GameplayEffectDefinition` | 状态与表现数据资产 | `Assets/Combat/Effects/` |
| `GameplayTagCatalog` | Effect Tag → Asset 索引 | `Assets/GameplayTagCatalog.asset` |
| `CardManager` | 持有 Catalog；卡牌解锁参数 | 场景内对象 |
| `CardData` | 构筑词条 | 卡池 SO |

---

## 1. 配置原则

1. **一技能一类**：每个独立玩法用独立 `EnemyAbility_*` 脚本，不靠枚举塞进通用原型。
2. **输入只认 `type`**：
   - `BasicAttack` = 左键普攻
   - `Skill` = Q
   - `Mobility` = Space
   - `Passive` = 常驻
3. **卡牌只认 Tag**：用 `abilityTags` / `CardData.targetAbilityTags`，禁止用显示名或 Prefab 名匹配。
4. **伤害走共享入口**：命中必须调用 `DealDamageTo` / `DealDamageToPlayer`，才能吃到吸血、倍率、`appliedEffectTags`。
5. **有自定义 Mobility 时不要再依赖通用冲刺**：`MonsterActor` 若检测到子物体上已有非 `EnemyAbility_MobilityDash` 的 Mobility，就不再自动挂共享冲刺。
6. **Prefab 建议解包成完整资产**：嵌套 Variant 容易丢 `Enemy` 引用；傲慢最终使用完整 `pride_new.prefab`。

---

## 2. 标准落地流程（建议严格按序）

### Step A — 读需求，拆三槽

从 CSV 拆出：

| 槽位 | 问什么 |
|---|---|
| 普攻 | 攻击形态？是否含位移？伤害/冷却/耗血？ |
| 位移 | 纯位移还是位移+伤害？ |
| Q | 多段？选敌规则？自身状态？命中表现？卡牌成长？ |

**傲慢拆分结果（重要）**：旧 `pride(full)` 普攻 `FlashSlash` = 位移+攻击。新需求要求：

- 位移进 Space → `PrideChargeStrike`
- 攻击进普攻 → `SwordQi`
- Q 新增 → `PrideBlinkChain`

### Step B — 找/写 Ability 脚本

优先复用已有类；没有再新建。命中逻辑里：

```csharp
DealDamageTo(target, damage * multiplier); // 自动 Apply appliedEffectTags
```

自身持续状态用字段直接引用 Effect，或走 `activationEffect`：

```csharp
owner.Combat.ApplyEffect(untargetableEffect, owner.Combat, abilityTags, out _);
// ... 技能结束
owner.Combat.RemoveEffect(untargetableEffect);
```

### Step C — 创建 Gameplay Effect 资产

`Create > Possession/Combat/Gameplay Effect`，放到 `Assets/Combat/Effects/`。

两类常见用途：

| 用途 | 示例 | 关键配置 |
|---|---|---|
| 自身持续状态 | `Effect_Defense_Untargetable` | `duration<=0`（技能手动 Remove）；`grantedTags=State.Defense.Untargetable`；残影/拖尾放 `activeVfxPrefab` |
| 命中反馈 | `Effect_Combat_PrideBlinkHit` | 短 `duration`；受击特效放 `hitVfxPrefab` |
| 测试永久免伤 | `Effect_Defense_DamageImmune` | `duration<=0`；`grantedTags=State.Defense.DamageImmune`；`IncomingDamageMultiplier=0` |

`State.Defense.Untargetable` 已被框架接入：`CanDamage` 与 `TakeDamage` 都会忽略带该 Tag 的目标。  
`State.Defense.DamageImmune`：`TakeDamage` 直接 return，附身衰减也会跳过（作弊附身用，见 §10）。

### Step D — 登记 Catalog + 挂到场景

1. 打开 `Assets/GameplayTagCatalog.asset`
2. `declaredTags` 加入 Ability / Effect / State Tag
3. `effectDefinitions` 拖入对应 Effect 资产
4. 场景 `CardManager.gameplayTagCatalog` 指向该 Catalog  
   （`appliedEffectTags` 运行时靠 `CardManager.TryGetGameplayEffect` 解析；Catalog 为空则命中 Effect/VFX 不生效）

### Step E — 挂到怪物 Prefab

推荐结构：

```text
pride_new                    ← Enemy + CombatAbilityComponent
└─ abilities/
   ├─ basic_swordqi          ← EnemyAbility_SwordQi      type=BasicAttack
   ├─ mobility_charge        ← EnemyAbility_PrideChargeStrike  type=Mobility
   ├─ skill_blinkchain       ← EnemyAbility_PrideBlinkChain    type=Skill
   └─ pride_passive          ← EnemyPassiveBuff               type=Passive
```

在 `Enemy` 组件上确认列表与耗血：

| 列表 | hpCost（附身时） |
|---|---:|
| `basicAbilities` → SwordQi | 5 |
| `skillAbilities` → BlinkChain | 20 |
| `mobilityAbilities` → ChargeStrike | （当前 Mobility 列表无 hpCost 字段） |

每个 Ability 至少填：`abilityName`、`type`、`damage`、`cooldown`、`abilityTags`；需要命中 Effect 的填 `appliedEffectTags`。

### Step F — 卡牌（可选）

`CardData`：

```text
effectId: Pride.BlinkCountPlusTwo
targetAbilityTags: Ability.Monster.Pride.BlinkChain
abilityParameters:
  - key: BlinkCount
    value: 6
```

Ability 内用 `GetCardParameter("BlinkCount", blinkCount)` 读取。  
对应 `upgrades` 里加同名 `effectId` Slot。

### Step G — Play Mode 验收

优先用 §10 的作弊热键做快速验收；正式流程也可用常规附身。

1. 附身怪物：左键 / Q / Space 各对应正确 Ability
2. 无敌人时 Q 不可释放（若 Ability 覆写了 `CanTrigger`）
3. 多敌人时 Q 会换目标；单敌人时连打同一目标
4. Q 期间自身不可被选中/受伤，只有残影拖尾
5. 每次命中有扣血 + 受击特效
6. 卡牌解锁后数值/行为变化符合 `abilityParameters` / `IsUpgradeUnlocked`
7. 需要时打开命中盒调试（见 §8）

---

## 3. Ability / Tag / Effect 字段速查

### 3.1 EnemyAbility 通用字段

| 字段 | 用途 |
|---|---|
| `abilityName` | UI 显示名，不参与匹配 |
| `type` | 输入槽位 |
| `damage` / `cooldown` | 基础数值；冷却受 `attackSpeed` 修正 |
| `abilityTags` | 稳定身份，卡牌定位用 |
| `upgrades` | `effectId` 解锁槽 |
| `appliedEffectTags` | 经 `DealDamageTo` 命中后附加的 Effect Tag |
| `activationEffect` | 施法开始时施加到自身的 Effect |
| `requiredTags` | 释放前置 Tag |
| `vfxPrefab` 等 | 释放瞬间表现 |
| `drawHitboxes` | 单技能命中盒调试 |

### 3.2 Tag 命名

```text
Ability.Monster.<Sin>.<Skill>
Ability.Monster.Pride.SwordQi
Ability.Monster.Pride.ChargeStrike
Ability.Monster.Pride.BlinkChain

State.Control.Stunned / Rooted
State.Defense.Untargetable / Flying / DamageImmune
Effect.Control.Stunned
Effect.Defense.Untargetable
Effect.Defense.DamageImmune
Effect.Combat.PrideBlinkHit
```

`State.Defense.DamageImmune`：测试用永久免伤（见 §10）。`TakeDamage` 与附身衰减都会忽略带该 Tag 的目标。

### 3.3 Effect 表现字段

| 字段 | 时机 |
|---|---|
| `activeVfxPrefab` | Effect 存续期间挂在目标上 |
| `applyVfxPrefab` / `expireVfxPrefab` | 施加 / 移除时各播一次 |
| `weaponVfxPrefab` | 攻击施加时在武器锚点 |
| `hitVfxPrefab` | 攻击施加时在命中点 |
| `weaponVfxLifetime` / `hitVfxLifetime` | `FixedDuration` 或 `EffectLifetime` |

攻击命中常用组合：**伤害走 `DealDamageTo`，受击特效走命中 Effect 的 `hitVfxPrefab`，自身姿态走专用 Effect 的 `activeVfxPrefab`。**

### 3.4 卡牌参数约定

| key | 含义 | 读取方 |
|---|---|---|
| `ChargeDistance` | 突进距离 | PrideChargeStrike 等 |
| `BlinkCount` | 穿梭次数 | PrideBlinkChain |
| `SummonCount` | 召唤数量 | SlothDrone / LustSummon |
| `TransformDuration` | 形态时长 | GluttonySmallCat |
| `FlightDuration` | 飞行时长 | EnvyFlight |
| `ExecuteThreshold` | 斩杀血量比例 | GluttonyDevour |
| `BladeCount` | 环绕飞刀数 | ReturningBladeOrbit |

---

## 4. 范例：傲慢-终刃绝影是怎么做的

### 4.1 需求对照

| 槽位 | CSV 需求 | 落地 |
|---|---|---|
| 被动 | +5% 移速 | 保留 `EnemyPassiveBuff` |
| 位移 | 一刀斩突进，耗血，伤害 20 | `EnemyAbility_PrideChargeStrike` |
| 普攻 | 短程剑气，伤害 20 | `EnemyAbility_SwordQi`（从旧 Skill 改到 BasicAttack） |
| Q | 穿梭 4 次 / 0.25s；不可选中；无敌人不放；单目标连打；每次扣血+受击特效 | `EnemyAbility_PrideBlinkChain` |

旧 `pride(full)` 的 `FlashSlash`（位移+攻击普攻）**删除**，职责拆开。

### 4.2 用到的脚本

| 脚本 | type | 关键逻辑 |
|---|---|---|
| `EnemyAbility_SwordQi` | BasicAttack | 朝瞄准方向发射短程剑气 |
| `EnemyAbility_PrideChargeStrike` | Mobility | 朝前冲，路径/落点伤害；读 `ChargeDistance` |
| `EnemyAbility_PrideBlinkChain` | Skill | 见下节 |

### 4.3 Q：`EnemyAbility_PrideBlinkChain` 行为

1. `CanTrigger`：附近没有可伤害敌人 → 直接失败  
2. 对自身 `ApplyEffect(untargetableEffect)`  
3. `hideOwnerMeshes=true` 时隐藏 Mesh/SkinnedMesh，只留 Effect 残影拖尾  
4. 循环 `BlinkCount`（默认 4）次：  
   - 闪现到目标旁  
   - `DealDamageTo`（伤害 + `appliedEffectTags`）  
   - 以当前目标为原点找最近**其他**敌人；没有则继续打当前目标  
   - 等待 `blinkInterval`（0.25）  
5. `RemoveEffect` + 恢复 Mesh + `EndActivationEffect`

### 4.4 Effect 资产（已创建）

**`Assets/Combat/Effects/Effect_Defense_Untargetable.asset`**

```text
effectTag: Effect.Defense.Untargetable
duration: 0                    # 永久，由技能结束时 Remove
grantedTags: State.Defense.Untargetable
activeVfxPrefab: 拖尾/残影 Prefab
parentVfxToTarget: true
```

**`Assets/Combat/Effects/Effect_Combat_PrideBlinkHit.asset`**

```text
effectTag: Effect.Combat.PrideBlinkHit
duration: 0.05
hitVfxPrefab: 受击特效 Prefab
hitVfxLifetime: FixedDuration
attackVfxDuration: 0.3
```

二者都登记在 `GameplayTagCatalog`，并由 `CardManager` 引用。

测试免伤（作弊附身自动施加）：

**`Assets/Combat/Effects/Effect_Defense_DamageImmune.asset`**

```text
effectTag: Effect.Defense.DamageImmune
duration: 0
grantedTags: State.Defense.DamageImmune
modifiers: IncomingDamageMultiplier = 0
```

### 4.5 Prefab Inspector 最终值（`pride_new`）

| 节点 | 组件 | 关键值 |
|---|---|---|
| 根 | `Enemy` | `displayName=傲慢-终刃绝影`，`maxHealth=100`，`moveSpeed=10` |
| `basic_swordqi` | SwordQi | `type=BasicAttack`，`damage=20`，`cooldown=1`，`maxRange=4`，`abilityTags=Ability.Monster.Pride.SwordQi`，hpCost=5 |
| `mobility_charge` | PrideChargeStrike | `type=Mobility`，`damage=20`，`cooldown=2`，`chargeDistance=5`，`abilityTags=Ability.Monster.Pride.ChargeStrike` |
| `skill_blinkchain` | PrideBlinkChain | `type=Skill`，`damage=10`，`cooldown=1.5`，`blinkCount=4`，`blinkInterval=0.25`，`untargetableEffect=...Untargetable`，`appliedEffectTags=Effect.Combat.PrideBlinkHit`，`hideOwnerMeshes=true`，hpCost=20 |

卡牌「穿梭+2」示例：

```text
effectId: Pride.BlinkCountPlusTwo
targetAbilityTags: Ability.Monster.Pride.BlinkChain
abilityParameters: [{ key: BlinkCount, value: 6 }]
```

### 4.6 傲慢 Build 词条（已写入 `CardLibrary`）

热键顺序 = `CardLibrary` 中匹配该技能 Tag 的卡牌顺序（1 起）。CSV 对照如下：

| 技能槽 | 热键 | effectId | 行为 |
|---|---|---|---|
| 普攻 | LMB+1 | `Pride.Pierce` | 穿透；首次命中后打出 X 字气刃；伤害 ×2 |
| 普攻 | LMB+2 | `Pride02` | 三道散射 |
| 普攻 | LMB+3 | `Pride01` | 气刃变远程（`pride01MaxRange`） |
| 位移 | Space+1 | `Pride.ChargeEmpowered` | `ChargeDistance=10`；距离加成折算最多 +100% 伤害 |
| Q | Q+1 | `Pride.BlinkSwordQi` | 每次穿梭后沿穿梭方向追加一发剑气 |
| Q | Q+2 | `Pride.BlinkCountPlusTwo` | `BlinkCount=6`（默认 4） |

`pride_new` 各 Ability 的 `upgrades` 槽必须包含对应 `effectId`。卡牌一律填 `targetAbilityTags`，不要依赖 `abilityPrefab` 显示名回退。

### 4.7 从旧 Prefab 迁到新 Prefab 的操作清单

1. 复制/解包旧怪物为 `xxx_new.prefab`（避免深层 Variant）  
2. 删掉职责混乱的旧 Ability（傲慢删 `FlashSlash`）  
3. 按槽位加/改三个 Ability 子物体  
4. 配 `abilityTags` / Effect 引用 / 数值 / hpCost  
5. 建 Effect → 登记 Catalog → 挂 CardManager  
6. 在 `CardLibrary` 补词条，Ability `upgrades` 对齐 `effectId`  
7. Play Mode 按 §2 Step G / §10 验收  

---

## 5. Ability 类与需求映射

| Ability 类 | 怪物需求 | 核心字段 | 卡牌参数 |
|---|---|---|---|
| `EnemyAbility_PrideChargeStrike` | 傲慢一刀斩 | `chargeDistance`、`chargeSpeed`、`hitRadius`、`landingRadius` | `ChargeDistance` |
| `EnemyAbility_PrideBlinkChain` | 傲慢穿梭斩 | `searchRange`、`blinkCount`、`blinkInterval`、`untargetableEffect`、`hideOwnerMeshes` | `BlinkCount` |
| `EnemyAbility_SwordQi` | 傲慢普攻剑气 | `maxRange`、`projectileSpeed`、散射/穿透升级 | `Pride.Pierce` / `Pride01` / `Pride02` |
| `EnemyAbility_SlothLaunch` | 怠惰弹射 | `windupDuration`、`launchDistance`、`landingRadius` | `LaunchDistance` |
| `EnemyAbility_SlothDrone` | 怠惰无人机 | `dronePrefab`、`droneCount`、`droneLifetime` | `SummonCount` |
| `EnemyAbility_GluttonySmallCat` | 暴食变猫 | `formDuration`、`smallCatEffect` | `TransformDuration` |
| `EnemyAbility_GluttonyDevour` | 暴食吞噬 | `range`、`angle`、`executeHealthFraction` | `ExecuteThreshold` |
| `EnemyAbility_EnvyFlight` | 嫉妒飞行 | `flightDuration`、`flightEffect` | `FlightDuration` |
| `EnemyAbility_WrathSelfGrapple` | 愤怒自钩 | `grappleDistance`、`grappleSpeed` | `GrappleDistance` |
| `EnemyAbility_ReturningBladeOrbit` | 贪婪环绕飞刀 | `bladePrefab`、`bladeCount`、`orbitDuration` | `BladeCount` |
| `EnemyAbility_LustSummon` | 色欲召唤 | `minionPrefab`、`summonCount`、`minionLifetime` | `SummonCount` |

### 5.1 贪婪环绕飞刀补充

| 字段 | 建议 |
|---|---|
| `bladePrefab` | 飞刀/鬼手投射物 |
| `bladeCount` | 默认 3 |
| `orbitRadius` / `orbitDuration` | 环绕几何与时长 |
| `hitRadius` / `hitInterval` | 碰撞与同目标间隔 |
| `maxSplitCount` / `returnSpeed` | 分裂与返航 |

复杂弹道留在该组件内；卡牌只改数量等数值。

---

## 6. 各怪物落地状态

### 傲慢：终刃绝影 — **已按本指南落地（`pride_new`）**

- 普攻：`SwordQi` + 词条穿透/散射/远程
- 位移：`PrideChargeStrike` + 词条蓄力突进
- Q：`PrideBlinkChain` + Untargetable / BlinkHit Effect + 词条附加气刃 / 穿梭+2
- 待做：正式幻影残影模型替换 Untargetable 的 `activeVfxPrefab`；穿透 X 字可用专用 VFX 替换当前双臂气刃实现

### 怠惰：机械之灵

- 位移：`SlothLaunch`；普攻：`ChargeShot`；Q：`SlothDrone`

### 暴食：魔猫

- 位移：`GluttonySmallCat`；普攻：深渊巨口（待专用 Ability）；Q：`GluttonyDevour`

### 嫉妒：激光异形

- 位移：`EnvyFlight`；普攻：`Laser`；Q：`ChainLightning` + 眩晕 Effect

### 愤怒：链狱冥兽

- 位移：`WrathSelfGrapple`；Q：既有链拉 + 范围表现

### 贪婪：魔鬼手

- 普攻：`TKDagger`；Q：`ReturningBladeOrbit`

### 色欲：灵念师

- Q：`LustSummon`；位移/普攻需求表未定

---

## 7. 命中盒调试

1. 场景建空物体，加 `CombatHitboxDebugSettings`，勾选 `enableHitboxDebug`
2. 目标 Ability 勾选 `drawHitboxes`
3. Play Mode 下在 Scene View 查看线框

已覆盖共享球/盒/扇形/路径胶囊，以及剑气、蓄力射击、飞轮、飞刀、地雷和多数新原型技能。

---

## 8. 验收清单

- [ ] Prefab 下每个战斗技能都有明确 `type`（BasicAttack / Skill / Mobility）
- [ ] 每个 Ability 有唯一 `abilityTags`
- [ ] 自定义 Mobility 存在时，根上没有多余的共享 `MobilityDash`
- [ ] 命中伤害都走 `DealDamageTo` / `DealDamageToPlayer`
- [ ] 需要命中 Effect 的技能填了 `appliedEffectTags`，且 Catalog + CardManager 已接通
- [ ] 自身持续状态 Effect 有正确 `grantedTags`；不可选中类验证了不会被打中
- [ ] 持续表现用 `activeVfxPrefab`，命中反馈用 `hitVfxPrefab`
- [ ] `Enemy` 上 basic/skill 的 `hpCost` 与需求表一致
- [ ] 卡牌 `targetAbilityTags` / `effectId` / `abilityParameters` 与 Ability 读取 key 一致
- [ ] Ability `upgrades` 含全部相关 `effectId`
- [ ] Play Mode：无目标不放、多目标换打、单目标连打、输入槽位正确
- [ ] 用 §10 热键验证：附身、刷怪、词条解锁

---

## 9. 常见坑

| 现象 | 原因 | 处理 |
|---|---|---|
| 命中无受击特效 / Effect | Catalog 未登记或 CardManager 未赋值 | 查 §2 Step D |
| Space 同时冲刺两次 | 自定义 Mobility + 自动 MobilityDash | 确认自定义 Mobility 的 `type` 在运行前已是 Mobility |
| Q 能对空气释放 | 未覆写 `CanTrigger` | 参考 BlinkChain |
| 不可选中无效 | Effect 未 grant `State.Defense.Untargetable`，或未 Apply | 查 Effect 与技能 Apply/Remove |
| Variant 丢 Enemy / 列表引用变 null | 深层 Prefab Variant 覆盖错误 | 解包为完整 Prefab 再配 |
| 改了 `abilityName` 卡牌失效 | 用名字匹配 | 改用 `abilityTags` |
| 技能+数字提示「no matching build cards」 | CardLibrary 未填 `targetAbilityTags`，或 Ability Tag 不一致 | 对齐 Tag；热键按住技能键时左上角会列出可解锁词条 |
| 作弊附身仍掉血 | 未挂 `Effect.Defense.DamageImmune` / Catalog 未登记 | 查 §10；确认 `MonsterPossessionCheat.damageImmuneEffect` 已赋值 |
| 数字键无响应 | 选卡 UI 等把 `PlayerController.IsGameplayInputBlocked` 置真 | 关掉弹窗后再测；`GameManager.testDoublePickOnStart` 可关 |

---

## 10. 测试流程（作弊热键）

开发期在 `CombatTest` 用数字键快速验证技能与 Build，不必走正常倒地附身。

### 10.1 场景准备

1. 打开 `Assets/Scenes/CombatTest.unity`
2. 场景内应有：
   - `MonsterPossessionCheat`（脚本 `Assets/Scripts/Debug/MonsterPossessionCheat.cs`）
   - `catalog` → `Assets/Configs/MonsterCheatCatalog.asset`
   - `damageImmuneEffect` → `Assets/Combat/Effects/Effect_Defense_DamageImmune.asset`
   - `CardManager.cardLibrary` → `Assets/Configs/CardLibrary.asset`
   - `CardManager.gameplayTagCatalog` → `Assets/GameplayTagCatalog.asset`
3. Catalog 位次当前为：`1 Pride(pride_new)` · `2 Envy` · `3 Wrath` · `4 Sloth` · `5 Lust`  
   新怪物：在 `MonsterCheatCatalog.monsters` 追加 Prefab 即可占下一个数字键。
4. Play Mode；若开局双选卡弹窗挡住输入，先关掉，或关闭 `GameManager.testDoublePickOnStart`

左上角 OnGUI 会显示怪物位次、当前按住技能对应的词条列表、以及最近一次作弊操作结果。

### 10.2 热键一览

| 输入 | 行为 |
|---|---|
| `1`–`9` | 按 Catalog 位次生成怪物并**立刻附身** |
| `0` | 从 Catalog **随机**刷一只敌方怪（不附身，可当靶子） |
| 附身中：按住 `LMB` + `1`–`9` | 解锁当前身体 **BasicAttack** 的第 N 个词条 |
| 附身中：按住 `Q` + `1`–`9` | 解锁 **Skill** 的第 N 个词条 |
| 附身中：按住 `Space` + `1`–`9` | 解锁 **Mobility** 的第 N 个词条 |

词条顺序 = `CardLibrary` 中 `targetAbilityTags` 匹配该技能 Ability 的卡牌顺序（1 起）。已解锁会提示 `Already unlocked`。

### 10.3 永久免伤（作弊附身专用）

数字键附身的身体会：

1. 施加永久 Effect `Effect.Defense.DamageImmune`（`duration<=0`）
2. 授予 `State.Defense.DamageImmune`
3. `IncomingDamageMultiplier = 0`

同时跳过附身衰减与技能耗血。结果：测试身体在任何情况下都不掉血，可长时间验证技能。

`0` 刷出的敌方怪**不会**上免伤，可被正常击杀。

相关资产：

```text
Assets/Combat/Effects/Effect_Defense_DamageImmune.asset
effectTag: Effect.Defense.DamageImmune
grantedTags: State.Defense.DamageImmune
duration: 0
modifiers: IncomingDamageMultiplier = 0
```

须登记进 `GameplayTagCatalog.declaredTags` 与 `effectDefinitions`。

### 10.4 推荐测试步骤（以傲慢为例）

1. Play → 按 `0` 刷几只靶子  
2. 按 `1` 附身 Pride；确认左上角提示 `(damage immune)`，站着挨打血量不变  
3. **普攻词条**  
   - 按住左键 + `1` → 穿透 / X 字 / 双倍  
   - 左键 + `2` → 三道散射  
   - 左键 + `3` → 远程  
   - 每次解锁后放左键，观察弹道与伤害变化  
4. **位移词条**  
   - 按住 Space + `1` → 蓄力突进  
   - Space 验证距离与伤害提升  
5. **Q 词条**  
   - 按住 Q + `1` → 穿梭附加气刃  
   - Q + `2` → 穿梭 6 次  
   - 对多/单目标各放一次 Q，确认换目标、附加剑气、次数  
6. 需要换怪时再按 `2`–`5`；旧作弊身体默认会回收  

### 10.5 给新怪物加测试位

1. Prefab 配好 Ability / Tag / Effect（§2）  
2. `CardLibrary` 追加该怪卡牌，填好 `targetAbilityTags`  
3. Ability `upgrades` 写入对应 `effectId`  
4. `MonsterCheatCatalog` 追加一条（displayName + prefab）  
5. 按 §10.4 同款流程验收三槽位与词条  

### 10.6 正式验收 vs 作弊验收

| 项目 | 作弊热键 | 正式流程 |
|---|---|---|
| 附身入口 | `DebugForcePossess` 跳过倒地窗口 | 击杀掉血 → 右键附身飞行 |
| 血量 | 永久免伤 | 衰减 + 技能耗血 |
| 词条获取 | 技能键+数字立刻解锁 | 房间 Core 选卡 / `CardManager.SelectCard` |
| 用途 | 迭代技能与 Build | 玩法与节奏验收 |
